# ADR process

Use the next four-digit number. Record context, decision, consequences, owner and date. Accepted ADRs are immutable; supersede them with a new ADR and cross-links. A parallel session proposes shared changes through a contract-gap document and may not silently edit an accepted contract.
