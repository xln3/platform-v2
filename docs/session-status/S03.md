# S03 — Product experience

Updated: 2026-07-24 23:40 (Asia/Shanghai)  
Status: in progress — customer account contract unblocked; S02 and external qualification pending

## Completed

- Rechecked the live and frozen OpenAPI after S01's latest delivery: the schema grew from 36 to 44 paths and now
  contains the customer-safe platform-account list/registration, authorization, pairing list/create, event list
  and revocation workflow endpoints.
- Regenerated the customer boundary from OpenAPI path types and added generated-client wrappers for all seven
  customer account operations. HTTP 401/403/404 all fail closed to the same forbidden presentation so an
  unauthorized role cannot infer account existence.
- Replaced the Customer account fixture for validated live sessions. The live branch now registers an account,
  creates its authorization, creates and polls a controlled-terminal pairing, projects safe event summaries and
  starts revocation without inventing a completion receipt. Development sessions remain visibly labelled fixtures.
- Added a hostile-response DLP E2E in which otherwise successful account/event responses include Cookie, token,
  OTP, full-phone and profile-path fields. Only the allow-listed safe projection reaches React state; no canary
  reaches DOM, storage, URL or captured writes/diagnostics.
- Verified the S01 customer router directly against FastAPI/Postgres: 3/3 integration tests passed. The generated
  API client has 7/7 tests, Customer Testing Library has 9/9, and the complete frontend unit suite has 17/17.
- Updated the nine Customer account/pairing/native-challenge screenshots at 1600×1100, 1024×768 and 390×844.
  The customer account Playwright suite passes 21/21 across those viewports. The first full run reached 186/189;
  its only failures were one obsolete ambiguous test locator repeated at three viewports. After narrowing the
  locator, the complete production-browser suite passes 189/189.
- Added an opt-in `GEO_VITE_NO_WATCH=1` build mode to all four applications because the shared host has exhausted
  its inotify instance quota. Sequential production builds pass without modifying host sysctls or other processes.
- Fully read the consolidated plan, V2 implementation plan, implementation status, comparison report, S00 evidence,
  ownership, frozen conventions/ADRs/OpenAPI, client workspace design and the complete account/session custody plan.
- Audited all four React applications and S03-owned shared packages. Baseline was four static health-probe welcome
  pages; the frozen OpenAPI currently has only health/readiness, project listing and collection-run creation.
- Replaced the welcome page with a shared responsive product shell, semantic navigation, skip link, project context,
  action header, metric cards, table/card patterns, badges and unified product status panels.
- Implemented distinct `loading`, `empty`, `real-zero`, `insufficient`, `failed`, `delayed`, `forbidden` and `ready`
  presentation semantics with local retry support.
- Added a narrow `AccountSummaryProjection` and `AccountSummary` that expose only masked account and lifecycle
  summaries, explicitly prohibiting secrets. `adapter_ready` is labelled “未经 live 验证”.
- Exported `AuthorizationScope`, `CustodyMode`, `SessionHealth`, `AdmissionLevel`, `InterventionStatus` and
  `RevocationReceipt`. Added an allow-list projection boundary that discards all unknown API properties before
  account data reaches UI/cache/URL/telemetry.
- Added the interactive Customer account lifecycle:
  registration summary → custody mode → one-time limited pairing → second confirmation → native-platform
  challenge → complete/refuse/timeout → health result → revocation receipt. It never asks for OTP, Cookie or token.
- Installed the fixed-stack frontend dependencies that were absent from the S00 skeleton: TanStack Query/Table,
  React Hook Form, Zod/resolvers, ECharts, React Flow, PDF.js, Konva/react-konva, Testing Library and Playwright.
- Added browser-DOM interaction coverage for pairing completion, refusal, timeout and revocation, including checks
  that no secret input exists and no DLP canary reaches DOM, URL or browser storage.
- Added Playwright infrastructure and verified the complete account pairing→verification→revocation flow at
  1600×1100, 1024×768 and 390×844. Console errors and failed requests are asserted to be zero.
- The first tablet run found hidden navigation labels also removed accessible names. Added explicit nav
  `aria-label`; the three-viewport rerun passed.
- Added shared `ExperienceProvider` with tenant/project/user/role context, one TanStack Query client per application,
  consistent retry/staleness defaults and a product Error Boundary.
- Added recursive client-diagnostic redaction before telemetry/error reporting; secret-named keys and secret-looking
  values are removed while safe request context remains.
- Wrapped all four application roots in role-appropriate experience contexts.
- Customer monitoring now uses TanStack Table and React Router `useSearchParams` for allow-listed time-window,
  model and answer-mode filters. Browser history restores each filter state.
- Replaced Customer profile, brand/product/competitor and question/goal/change-request placeholders with real
  React Hook Form + Zod workflows:
  - profile truth confirmation, version generation and separated customer/AI/history provenance;
  - HTTPS official-site validation and customer-confirmed brand/product/competitor registration;
  - question priority, metric target and pause/resume/backfill/add-query requests that enter review rather than
    mutating scheduling truth.
- Added responsive form, validation, history, asset-list and request-queue patterns. Visual review found full-page
  screenshot stitching exposed the negative-offset skip link; switched to standard clipped hiding with focus reveal.
- Replaced the remaining Customer placeholders:
  - project-stage home, single recommended next action, latest KPIs and honest delayed-data state;
  - paginated AI answers with model/mode/region/question URL filters, sources, answer screenshot, bbox/text anchor,
    history diff and downloadable secret-free evidence-package manifest;
  - published-report center with version-bound questions, receipt confirmation, historical versions and real
    filtered CSV download; unavailable PDF remains disabled instead of fabricating an artifact;
  - customer member invitation/role management with masked email projections.
- Reworked Playwright into app-specific Customer and Operations projects at all three viewports. The root
  `pnpm test:e2e` now starts both correct application servers and no longer routes S01 tests through Customer.
- Established vertical application experiences:
  - Customer: KPI/model/question views and platform-account authorization/custody/pairing/revocation entry.
  - Operations: overall health, human intervention, authorization/lease, quarantine and revocation states without
    modifying S01 execution-feature business files.
  - Report Studio: frozen window, KPI trace, section provenance and review-oriented workspace.
  - Intelligence: claim/source matrix, uncertainty and accessible tabular propagation alternative.
- Selected Konva for evidence screenshot annotation and recorded the shared-ADR proposal in the contract gap.
- Completed the Report Studio vertical workflow: immutable data-window freeze, KPI contribution trace, AI/human
  chapter provenance, accessible Konva evidence annotation, paginated preview, comments/release gates, publication
  state and optimization-effect review.
- Switched Playwright web servers from cold Vite development transforms to production builds served by Vite
  preview. This removed the first-viewport `504 Outdated Optimize Dep` race while keeping failed-request assertions.
- Completed the Intelligence vertical workflow: case queue, atomic Claim matrix, source independence and same-origin
  clustering, React Flow propagation view with an equivalent accessible table, immutable page/visual history,
  probability interval/rule explanation, human verdict, version-preserving appeal and safe evidence manifest.
- Replaced the report preview placeholder with real lazy-loaded PDF.js parsing, worker output and canvas rendering;
  the equivalent semantic report copy remains available to assistive technology.
- Added WCAG 2 A/AA axe coverage for every application at all three target viewports. Fixed document titles,
  progress semantics, mobile scroll-region keyboard access, React Flow hidden-focus behavior and attribution
  contrast. The complete accessibility matrix passes 12/12.
- Added the visual data-state matrix for normal/loading/empty/real-zero/insufficient/failed/delayed/forbidden,
  including local retry behavior. It passes axe and screenshots at all three viewports.
- Implemented the previously type-only shared packages:
  - `@geo/charts`: lazy ECharts bar rendering plus an equivalent accessible table;
  - `@geo/evidence-viewer`: reusable screenshot/bbox/text-anchor/history-diff view;
  - `@geo/workflow-ui`: semantic workflow timeline.
    Customer monitoring/evidence and Report outcomes now consume these packages.
- Replaced the handwritten health request with an `openapi-fetch<paths>` generated-path client, regenerated the
  36-path schema/manifest and restored `pnpm check:api`.
- Added browser DLP negative coverage where the API deliberately returns Cookie/token/OTP/proxy password/full
  phone/profile path/biometric canaries; none reaches DOM text, storage, URL or captured diagnostics.
- Added high-load browser coverage for a 120-dimension ECharts view, 500-row table, long evidence text and a hostile
  multi-kilobyte URL. The URL is promptly allow-listed/redacted and all three viewports complete below the 10-second
  ceiling with zero runtime failures.
- Established and then independently rechecked 15 pixel baselines across the four applications at 1600×1100,
  1024×768 and 390×844. Every visual case also asserts the root document has no horizontal overflow.
- Completed the evidence dialog's keyboard contract: focus enters the close control, Tab/Shift+Tab remain trapped,
  Escape closes, and focus returns to the exact trigger. Corrected the neutral badge foreground to pass AA contrast.
- Re-ran the complete production-browser suite after the visual and focus changes: all 54/54 scenarios passed across
  the three target viewports, including runtime failure interception, DLP, large-data and visual comparisons.
- Replaced the Customer account authorization summary-only fixture with an editable RHF/Zod registration workflow:
  account owner, operational responsible person, read/query/draft/publish scopes, expiry, region and custody mode.
  Its allow-listed result updates the shared safe projection and is carried into the pairing confirmation; the form
  contains no password, OTP, Cookie or token field.
- Split the Operations non-execution shell into dedicated overview, session-health, human-intervention and lifecycle
  event views. They cover authorization expiry, leases, native challenges, quarantine, revocation events and a safe
  revocation receipt while leaving S01's execution feature and API calls untouched.
- Completed the Customer monitoring dimension audit: added an allow-listed URL-bound region filter, five-freeze-day
  trend, customer-confirmed competitor comparison and region × answer-mode table. Every chart retains the equivalent
  accessible data table and the region filter participates in browser-history restoration.
- Expanded Report Studio visual regression from its default data-window page to all seven workspaces: freeze,
  KPI trace, chapter editor, Konva evidence editor, real PDF.js preview, review/publish and outcome review at all
  three target viewports. The expansion exposed and fixed a real mobile root-overflow defect by allowing the evidence
  grid item to shrink while keeping the fixed-coordinate canvas inside its local scroll container.
- Expanded Intelligence visual regression to all seven workspaces at all three viewports: cases, Claim matrix,
  source clusters, React Flow propagation plus its table alternative, visual history, verdict/appeal and evidence
  package. All views assert root containment after their async visualization is ready.
- Expanded Operations visual regression to overview, safe session health, human intervention and lifecycle events
  at all three viewports; S01 execution remains separately exercised against its real APIs and was not modified.
- Expanded Customer visual regression to all nine planned workspaces at all three viewports: home, profile,
  brand/product/competitor, questions/goals/change requests, monitoring, answers/evidence, reports, members and
  account authorization/pairing. Each baseline waits for its semantic content (and async charts where relevant) and
  asserts no root-level horizontal overflow.
- Expanded axe from representative pages to every one of the 27 planned workspaces across all three viewports
  (plus the Customer evidence dialog). This found two selector-specific Badge contrast regressions in Customer asset
  and member rows and a non-focusable horizontally scrollable Konva region on tablet/mobile. Badge selectors now
  preserve tone colors, and the evidence canvas is keyboard focusable and scrollable.
- Added a generated-contract identity boundary to `@geo/api-client`: it validates `/identity/session` before
  requesting `/projects`, returns a uniform forbidden result for 401/403, and never sends a service token for a
  human browser session. Session, project and header types all come directly from generated OpenAPI paths.
- Wired that generated identity/project boundary into all four application roots through
  `ValidatedExperienceProvider`. Only allow-listed, non-secret tenant/actor/role runtime hints may initiate the
  request; partial or secret-looking hints are purged and fail closed. Explicit unavailable/forbidden responses
  never fall back, while the no-hint development path remains visibly identified as `contract-fixture`.
- Added three-viewport role-isolation browser coverage: unauthorized sessions render the same forbidden surface
  before a project probe, a validated session adopts the live project context, and a full-phone runtime canary is
  purged before the identity request. All 9/9 cases pass.
- Probed the available real API at `127.0.0.1:45200` using safe S01 E2E tenant/actor/admin headers:
  `/api/v2/identity/session` returned the generated admin session projection and `/api/v2/projects?limit=1`
  returned `S01 E2E Project`, both HTTP 200. No Cookie, bearer token, OTP or service token was supplied.
- Hardened the shared identity projection against secret-shaped values hidden inside otherwise valid generated
  fields. Unsafe tenant/user identifiers fail closed; unsafe project names become a neutral `未命名项目`; unknown
  response properties are never copied. This closes the path from a hostile successful identity/project response
  into application context, DOM, browser storage or URL.
- Added the complete proposed Konva decision record at
  `docs/contract-gaps/S03-ADR-0005-konva-evidence-annotation.md`. It records context, decision, consequences,
  rejected Fabric.js alternative, accessibility/security constraints, validation and S04 promotion instructions
  without mutating the S00-owned accepted ADR directory.
- Promoted pagination, modal dialog and toast from application-specific markup/styles into reusable design-system
  components. Customer evidence and account authorization now consume them. The shared dialog owns modal
  semantics, initial focus, focus trap, Escape/backdrop close and exact trigger-focus restoration.
- Added reusable `FormField`, `FilterBar` and keyboard-focusable `TableRegion` design-system primitives and
  replaced Customer's local form wrapper plus monitoring/evidence filter bars and responsive region × mode table
  wrapper. React Hook Form remains application-owned while labels, hints and error presentation stay consistent.
- Fixed filtered-answer pagination when a URL points at a page beyond the newly filtered result set. Rendering and
  controls now use the bounded page, and a replace navigation removes the stale `answer_page` value without adding
  a misleading history entry.
- Consolidated secret-shaped value detection into the design system and reused it for diagnostic/error redaction,
  safe account summaries, auth/session hints and Customer URL search. Secret values hidden inside otherwise
  allow-listed `ownerLabel`, account mask, expiry, verification, project or generic diagnostic message fields now
  fail closed or become neutral labels rather than reaching UI, cache, URL or telemetry.
- Added a cross-application product-role isolation matrix. Customer rejects operator, Operations rejects customer,
  Report Studio rejects operator and Intelligence rejects customer before any product shell or account lifecycle
  request renders. The common test also asserts project/account fixture labels remain absent and no
  platform-account/intervention/lease request occurs.
- Added direct shared Error Boundary tests. A real render exception containing a full phone, OTP and browser profile
  path produces only a redacted diagnostic, shows the safe fallback and recovers after explicit user retry.
  `StatePanel` also has a direct local-retry assertion.
- Added three-viewport browser recovery for an explicit identity-session 503: no fixture fallback or business
  content appears, the secret-bearing error body reaches no browser surface, and user retry reloads the boundary
  and adopts the successful live project context. Removed a duplicate outer `role=alert` found by the test so
  assistive technology receives one failure announcement.
- Extended diagnostic DLP to numeric values. Six-digit integer challenge codes and integer mainland-phone shapes
  are redacted even when hidden under generic object keys, while ordinary HTTP status and decimal metric values
  remain usable. This closes the JSON-number variant of the OTP/full-phone error-report leak.

## In progress

- S02 analytics, evidence, exports, reports and intelligence still expose zero product paths in the current
  generated OpenAPI. Continue replacing their explicitly labelled fixtures as their owner mounts those contracts.
- External-platform qualification still requires a real, authorized target account/credential supplied through the
  controlled S01 custody path; S03 neither possesses nor fabricates one.
- The customer registration contract binds owner and responsible person to the authenticated actor and does not
  accept a separately selected responsible person. The live UI states that binding truthfully; selectable
  responsibility remains a contract gap.

## Not completed

- Real S02 API integration, complete external-platform qualification and production V2. The customer-safe account
  projection is now mounted and integrated.
- Completion-gate audit:
  1. planned four-application pages and contract-backed critical interactions: implemented; final live-account
     qualification remains dependent on the missing customer projection/external platform;
  2. generated OpenAPI client with drift guard: passed for every currently mounted contract;
  3. contract E2E: passed for the mounted S01 Operations and customer-safe account APIs; S02 integration is waiting
     on product contracts that are not mounted;
  4. three-viewport visual, accessibility and error-state matrix: passed;
  5. browser runtime/DLP gates: passed under fixture and deliberate hostile-payload coverage;
  6. Customer/Operations lifecycle, role isolation and three-viewport coverage: mounted contract paths passed;
     final external-platform qualification remains unavailable;
  7. this status records tests, URLs, screenshots, contract gaps and risks.
- The persistent goal therefore remains active: gates 3 and 6 cannot be declared fully satisfied until the missing
  live contracts and an external-platform qualification target are available.

## Contract gaps

- `S03-001`: customer safe projections are resolved; S02 product APIs and selectable responsibility remain. See
  `docs/contract-gaps/S03-001-product-experience-api.md`.
- S01 already opened `S01-001` for identity/project/collection/account/intervention contracts.

## Modified files

- `packages/design-system/src/index.tsx`
- `packages/design-system/src/styles.css`
- `packages/design-system/src/security.test.ts`
- `packages/api-client/src/{index.ts,index.test.ts}`
- `packages/auth/src/{index.ts,index.test.ts}`
- `apps/{customer-web,operations-web,report-studio,intelligence-web}/app/shell.tsx`
- `apps/customer-web/app/shell.test.tsx`
- `apps/customer-web/vite.config.ts`
- `apps/{customer-web,operations-web,report-studio,intelligence-web}/app/root.tsx`
- `package.json`, `pnpm-lock.yaml`, affected application/package manifests
- `playwright.config.ts`
- `tests/e2e/customer-account.spec.ts`
- `tests/e2e-results/{results.json,customer-account-desktop.png,customer-account-tablet.png,customer-account-mobile.png}`
- `docs/contract-gaps/S03-001-product-experience-api.md`
- `docs/contract-gaps/S03-ADR-0005-konva-evidence-annotation.md`
- `docs/session-status/S03.md`

## Data migrations

- None. S03 does not own database migrations.

## Commands and results

- `pnpm typecheck`: all 11 TypeScript workspaces passed. The final shared Python mypy phase failed in
  S02-owned `domain/metrics/core.py:97` (`int | None` assigned to `int`); S03 frontend code has no type error.
- `pnpm build`: passed, 11/11 workspace tasks successful; all four React Router SPA builds generated
  `build/client/index.html`.
- `pnpm --filter @geo/design-system test`: 2/2 DLP allow-list tests passed.
- `pnpm --filter @geo/customer-web typecheck`: passed.
- `pnpm --filter @geo/customer-web build`: passed; React Router SPA output generated.
- `pnpm --filter @geo/customer-web test`: 3/3 Testing Library interaction tests passed.
- `pnpm test:e2e -- tests/e2e/customer-account.spec.ts`: 3/3 passed after fixing the tablet navigation accessible
  name. Chromium desktop/tablet/mobile completed in 7.7 seconds.
- Expanded Customer Playwright suite: 6/6 passed at all three viewports, including URL filter history restoration.
- `pnpm --filter @geo/design-system test`: now 3/3, including recursive telemetry/error-report redaction.
- `pnpm build`: passed again after provider/context/Table integration; 11/11 workspace tasks successful.
- Customer Testing Library suite now passes 6/6, covering profile confirmation, asset registration and configuration
  request validation/submission in addition to account lifecycle.
- Customer Playwright suite now passes 9/9 at all three viewports in 20.9 seconds. It asserts zero console errors
  and failed requests for account and form flows.
- Customer Testing Library suite now passes 9/9, adding pagination/evidence dialog, report question/confirmation and
  member-email masking.
- `pnpm test:e2e`: 15/15 passed in 28.2 seconds: 12 Customer flows and 3 S01 real-API Operations execution flows
  across 1600×1100, 1024×768 and 390×844.
- `pnpm --filter @geo/report-studio test`: 3/3 interaction tests passed.
- `pnpm exec playwright test reports-studio.spec.ts`: 3/3 production-build tests passed across all target
  viewports; console errors and failed requests are zero.
- `pnpm --filter @geo/intelligence-web typecheck && pnpm --filter @geo/intelligence-web build`: passed.
- `pnpm --filter @geo/intelligence-web test`: 3/3 interaction tests passed.
- `pnpm exec playwright test intelligence-workbench.spec.ts`: 3/3 production-build tests passed across all target
  viewports; console errors and failed requests are zero.
- `pnpm exec playwright test accessibility`: 12/12 WCAG A/AA tests passed across all applications/viewports.
- `pnpm exec playwright test customer-state-matrix.spec.ts`: 3/3 passed with state screenshots and axe.
- `pnpm turbo run typecheck --filter='./apps/*' --filter='./packages/*'`: 11/11 passed.
- `pnpm turbo run test --filter='./apps/*' --filter='./packages/*'`: 16/16 tasks passed; 22 executable frontend
  tests passed and the remaining type-only packages exited cleanly.
- `pnpm check:api`: passed after deterministic regeneration of the current 36-path OpenAPI and TypeScript client.
- Final `pnpm test:e2e`: 39/39 passed in 1.2 minutes after shared ECharts/evidence/workflow and performance/DLP
  additions. All four apps passed desktop/tablet/mobile with zero asserted console errors and request failures.
- `pnpm exec playwright test customer-performance.spec.ts`: 3/3 passed; 120 chart dimensions + 500 business rows +
  long text rendered in about 1.1 seconds per viewport and hostile URL canaries were removed.
- `pnpm lint`: passed all 11 TypeScript workspaces and the complete Ruff target set.
- `pnpm build`: passed 11/11; ECharts and PDF.js are lazy chunks and all React Router apps emitted SPA clients.
- `pnpm format:check`: S03-owned files are clean; the command remains red only for
  `deploy/s02/compose.pgvector.yaml` and `docs/session-status/S02-COMPLETION-AUDIT.md`, both outside S03 ownership.
- `pnpm check:legacy-routes`: reports pre-existing drift only in legacy `server/geosys/app.py`; S03 did not modify
  or restore the legacy file or its manifest.
- Real API role-isolation probe at `127.0.0.1:45200`: a customer-role request against both an existing account event
  path and a fabricated account ID returned the same HTTP 401 `membership_invalid` body and identical SHA-256
  (`4287ba…c32f`), so this unauthorized context cannot infer account existence.
- `pnpm exec playwright test` after Customer authorization registration and Operations lifecycle additions: 60/60
  passed in 1.4 minutes. This includes 12/12 axe WCAG A/AA cases, 15/15 unchanged visual comparisons,
  root-overflow assertions, three Operations safe-lifecycle/DLP cases and browser-level Customer refusal/timeout
  terminal-state coverage at every viewport.
- `pnpm lint && pnpm build && pnpm check:api`: all 11 frontend workspaces and Ruff passed, all four React Router SPA
  production builds passed, and the generated client still matches the currently mounted 36-path OpenAPI.
- Final monitoring-dimension rerun: `pnpm lint`, `pnpm build` and the complete Playwright suite passed; Playwright is
  now 63/63 in 1.5 minutes. Monitoring itself is axe-clean and has three new stable viewport baselines.
- Report Studio expanded visual gate: 21/21 baselines generated, then 21/21 passed without snapshot updates.
  Combined Report interaction/accessibility/visual rerun passed 27/27; design-system and Report typechecks plus the
  Report production SPA build passed after the mobile overflow fix.
- Intelligence expanded visual gate: 21/21 baselines generated and 21/21 passed without updates. Its combined
  interaction, axe and visual run passed 27/27 across desktop, tablet and mobile.
- Operations expanded visual gate: 12/12 baselines generated and passed without updates; combined shell
  lifecycle/DLP/accessibility/visual coverage passed 18/18.
- Customer expanded visual gate: 27/27 baselines generated and passed in the subsequent full-suite run.
- Final four-application production-browser run after all workspace visual expansions: 126/126 passed in
  1.9 minutes. Every planned workspace now has desktop/tablet/mobile pixel coverage; all cases retain zero asserted
  console errors and failed requests.
- Final `pnpm lint` and `pnpm check:api` passed after the visual expansion. The 17:54 API router audit still found no
  mounted S02 product routers.
- Full-workspace accessibility matrix passed 12/12 Playwright projects after fixes; each project traverses all of
  its application's workspaces, yielding 81 workspace × viewport axe scans with no WCAG A/AA violation. Customer's
  27 visual baselines were regenerated only where the corrected Badge colors changed pixels and then passed 27/27
  without updates. Final lint/Ruff and Report production build passed at 18:02.
- API-client identity tests pass 4/4, including a valid generated session/project chain and an unauthorized request
  that fails closed after one request without probing project existence. `pnpm check:api` remains green.
- Root-session integration rerun: the complete four-application Playwright suite passed 135/135 in 2.3 minutes.
  This includes all 81 viewport/workspace visual baselines, 81 workspace × viewport axe scans, role isolation,
  deliberate DLP payloads, Customer account lifecycle, performance stress and S01 real execution. Every applicable
  case asserts zero console errors and failed requests.
- Final `pnpm lint && pnpm build && pnpm check:api`: all 11 TypeScript workspaces and the full Ruff target passed;
  all 11 build tasks and four React Router SPA outputs passed; regenerated OpenAPI and generated TypeScript remain
  drift-free.
- `pnpm --filter @geo/auth test`: 5/5 new boundary tests passed, covering no-hint fixture fallback, partial/secret
  hint purge, explicit fail-closed behavior, generated-value allow-listing and worker rejection.
- Expanded role-isolation/DLP run passed 12/12 across all three viewports. A successful hostile API response
  deliberately included Cookie, full-phone, profile-path and proxy-password canaries; none reached DOM, storage or
  URL.
- Complete production-browser regression after the value-level DLP hardening passed 138/138 in 2.4 minutes with
  unchanged visual baselines and zero asserted console errors/failed requests.
- Shared pagination/dialog/toast validation: design-system typecheck and 9/9 Customer component tests passed;
  Customer interaction plus accessibility passed 18/18 across all three viewports; all 27 Customer workspace visual
  baselines passed unchanged.
- Final shared-component `pnpm lint && pnpm build && pnpm check:api`: 11/11 TypeScript/Ruff lint tasks, all four
  production SPA builds and deterministic OpenAPI regeneration/drift checks passed.
- Pagination/filter regression: Customer component tests pass 9/9, including page 2 → one-page region narrowing.
  The browser flow proves the correct answer remains visible, the control reports page 1/1 and the stale URL page is
  removed.
- Shared form/filter/table rerun passed 45/45 Customer interaction, full-workspace axe and visual cases across all
  three viewports. All 27 Customer pixel baselines remain unchanged.
- Repository lint/Ruff, all 11 build tasks/four production SPA outputs and OpenAPI drift check passed after the
  shared primitive migration.
- Value-level DLP tests pass: design-system 4/4, auth 5/5 and Customer 9/9. Coverage includes full mainland phone,
  OTP, Cookie/session, token, proxy password, arbitrary browser profile path and biometric material embedded in
  legitimate display/diagnostic fields.
- The first full-browser attempt correctly failed its startup gate because an orphaned S03 Intelligence preview
  returned HTTP 502 and a new Operations preview stopped responding. After verifying ownership and terminating only
  that stale `platform-v2` preview process group, the clean rerun passed 138/138 in 2.4 minutes with all visual,
  accessibility, DLP, role-isolation and real-S01 execution assertions intact.
- Final DLP hardening `pnpm lint && pnpm build && pnpm check:api` passed all 11 TypeScript/Ruff lint/build tasks, all
  four production SPA outputs and deterministic generated-contract drift verification.
- Cross-product role isolation passed 12/12 across desktop, tablet and mobile. The complete default Playwright suite,
  now including those tests through each application's normal project match, passed 150/150 in 2.4 minutes.
- Final role-matrix lint/Ruff and generated OpenAPI drift checks passed.
- Design-system recovery suite passes 6/6. Customer identity failure/retry plus full workspace accessibility passes
  18/18 at desktop, tablet and mobile.
- The default production-browser suite, now including explicit session failure/recovery, passed 153/153 in
  2.4 minutes. Final lint/Ruff, all 11 build tasks/four SPA outputs and OpenAPI drift verification passed.
- Numeric DLP canaries were added to the successful health response (`challenge` and generic `subject`, not
  secret-named keys). Customer lifecycle/browser-surface coverage passed 15/15 across all three viewports; neither
  number reached DOM, URL, local/session storage or captured console diagnostics.
- Design-system 6/6, final lint/Ruff, all 11 build tasks/four SPA outputs and OpenAPI drift verification pass after
  numeric-secret hardening.
- Hardened the Customer terminal-pairing confirmation so account mask, target platform, authorization scopes and
  region all come from the current allow-listed S01-safe projection instead of duplicated display constants.
  The paired stage now includes an accessible QR-shaped handoff affordance whose DOM, URL, screenshot and telemetry
  contain no pairing payload; the real one-time payload remains confined to the controlled terminal.
- Corrected the pairing completion copy to preserve the real `read_verified` admission level: it confirms only
  login/read verification and does not imply that registered draft/publish scopes have passed live qualification.
  The native-platform challenge still explicitly directs OTP, scan, Push MFA, passkey, face/liveness and graphical
  challenges to the platform-native page or controlled terminal and never renders a secret-entry field.
- Customer component coverage remains 9/9 and account lifecycle coverage passes 15/15 across desktop, tablet and
  mobile. The pairing confirmation, payload-free QR stage and native challenge are now traversed by the accessibility
  suite; the expanded Customer account/accessibility/visual run passed 33/33.
- Added and manually inspected six intermediate account visual baselines:
  `customer-account-pairing-qr.png` and `customer-account-native-challenge.png` at all three target viewports. The
  subsequent comparison run passed without snapshot updates and without horizontal overflow.
- The complete default production-browser suite, including the new pairing stages, passed 156/156 in 2.5 minutes.
  Final `pnpm lint && pnpm build && pnpm check:api` passed all TypeScript/Ruff lint tasks, all 11 build tasks/four
  SPA outputs, and deterministic generated OpenAPI/client drift verification at 19:13.
- Re-audited the live 36-path API and replaced the first eligible Customer fixture write with a real generated
  contract path. A validated live customer now submits “问题、目标与配置申请” to the mounted project
  `change-requests` resource with generated request/response types, a fresh idempotency key and its already
  validated S01 tenant/actor/role projection; fixture identities remain visibly local-only.
- Added generated-client list/create project-resource boundaries with explicit ready/forbidden/unavailable results.
  Error bodies cannot cross that boundary, service tokens are never emitted, and validated identity request headers
  remain memory-only and are cleared on fixture, unavailable, forbidden, unsafe or worker sessions.
- API-client tests pass 6/6 and auth tests pass 5/5. Three-viewport browser coverage captures the real-contract
  request and verifies its path, tenant/actor/role headers, idempotency key, payload and absence of secret material.
  Customer account/form coverage passes 18/18.
- Updated the three Customer question-workspace baselines to show the honest contract-fixture/live-write label;
  the subsequent Customer visual/accessibility comparison passed 33/33 with no overflow or axe violation.
- The complete default production-browser suite now passes 159/159 in 2.6 minutes. Final lint/Ruff, all 11 build
  tasks/four SPA outputs and generated OpenAPI drift checks passed at 19:25.
- Extended the shared string-level DLP detector to recognize standalone six-digit OTP values while preserving valid
  public IDs such as `tnt_live_123456`. Customer account authorization, company profile, brand assets, configuration
  requests, report questions and member invitations now reject secret-shaped ordinary-form content before an action.
- Applied the same pre-action guard to Report Studio chapter versions/review comments and Intelligence appeals.
  Buttons stay disabled with an accessible inline alert for Cookie, token/Bearer, OTP, password, full-phone or
  profile-path content; safe content remains editable and follows the existing version/review workflow.
- Three-viewport negative flows prove a pasted OTP never triggers the live Customer change-request API, secret
  chapter/comment content cannot be saved, and a secret appeal cannot be submitted. Design-system DLP passes 5/5,
  auth passes 6/6 (including numeric public-ID non-regression), and the focused Customer/Report/Intelligence browser
  runs pass 18/18 and 6/6.
- An intentionally interrupted Playwright run left four old S03 preview servers orphaned; their exact workspace PIDs
  were identified and terminated, then the current production bundles were rebuilt. The clean complete suite passed
  159/159 in 2.5 minutes, followed by green lint/Ruff, all 11 builds/four SPA outputs and OpenAPI drift checks at
  19:39.
- Replaced Report Studio's inert “保存章节版本” control with a real immutable in-session snapshot workflow. Each
  section maintains an independent monotonic version sequence, saving emits an accessible receipt with section name
  and version, and the semantic version history retains earlier snapshot lengths after later edits. Secret-shaped
  chapter content keeps both acceptance and version-save actions disabled.
- Report editing plus full Report accessibility passes 6/6 across desktop, tablet and mobile, including secret
  rejection followed by safe v1/v2 snapshots. The subsequent complete production-browser suite remains 159/159 in
  2.5 minutes; lint/Ruff, all 11 builds/four SPA outputs and OpenAPI drift checks passed at 19:44.
- Eliminated the remaining inert controls in Report Studio's critical editing path. KPI Trace now opens a named,
  version-frozen safe contribution-evidence region; the Konva editor exposes bounded keyboard-operable directional
  anchor adjustment with live bbox feedback and synchronized accessible canvas text; PDF.js preview now implements
  real “适合页面”/“100%” pressed states.
- At 100% zoom the fixed-width PDF is intentionally contained in the preview's local horizontal scroll region, so
  tablet/mobile retain the full artifact without creating root-page overflow. The dynamic evidence, anchor and zoom
  states are traversed by the Report axe suite at all three viewports.
- Report interaction/accessibility/visual coverage passed 27/27 after the changes. The complete production-browser
  suite passed 159/159 in 2.5 minutes with unchanged pixel baselines; lint/Ruff, all 11 builds/four SPA outputs and
  OpenAPI drift checks passed at 19:52.
- Replaced Intelligence Web's inert “检查证据锚点” control with a complete safe verification flow. Every filtered
  source can open a focus-trapped evidence dialog containing a screenshot/bbox alternative, text range, frozen hash,
  independence/same-origin classification and page-history diff; verification records a hash-bound event without
  mutating the original evidence and persists an “锚点已核验” status on the source card.
- The dynamic evidence dialog is traversed by the Intelligence axe suite, including Escape dismissal and focus
  restoration, while the workbench flow continues through the accessible propagation table, history, verdict and
  appeal. Intelligence interaction/accessibility/visual coverage passed 27/27 across all viewports.
- The complete production-browser suite passed 159/159 in 2.6 minutes with unchanged pixel baselines. Lint/Ruff,
  all 11 builds/four SPA outputs and generated OpenAPI drift checks passed at 19:57.
- Eliminated the remaining inert Customer critical-action controls. The home recommendation now performs a
  URL/history-backed transition to the report workspace; “打开证据中心” opens the anchored evidence/diff dialog;
  published-report online preview opens a version-labelled, hash-verified semantic report; and the account area
  provides a focus-trapped revocation sequence that never requests a secret.
- Completed Customer member management beyond invitation. Each masked member now has a named management dialog;
  authorized UI actions can promote/demote roles or remove a member with an audit receipt, while the last customer
  administrator is protected from demotion/removal. Full email addresses never enter the rendered member list,
  receipt or management state.
- Customer component tests pass 9/9. The expanded three-viewport account/delivery flow passes 18/18 and exercises
  history navigation, revocation guide, evidence center, online preview, last-admin protection, promotion and
  removal. All new dialog states are included in the Customer axe path; the combined Customer component/
  interaction/accessibility/visual run passed 60/60 with unchanged pixel baselines.
- The clean complete production-browser suite passed 159/159 in 2.6 minutes. Lint/Ruff, all 11 builds/four SPA
  outputs and generated OpenAPI drift checks passed at 20:06.
- Removed the four remaining inert controls from the shared `ProductShell`. The project switcher now opens a
  read-only safe tenant/project/session summary; notifications expose only non-inferential activity summaries;
  “导出视图” downloads a minimal secret-free JSON manifest; and “创建任务” presents explicit product-owned
  destinations without bypassing approval or fabricating a cross-domain task contract.
- The exported view deliberately omits the current URL, actor identity and all account/session material. Shared
  shell DLP coverage parses the downloaded artifact and rejects Cookie, bearer/token, OTP, proxy password, profile
  path and biometric terms, while all dialogs contain no secret input.
- Added a shared-shell interaction matrix for Customer, Operations, Report Studio and Intelligence across
  1600×1100, 1024×768 and 390×844. Its 12/12 scenarios verify dialog behavior, safe export, URL-backed task
  routing, zero console errors and zero failed requests. The three dynamic dialogs are also scanned in every
  app/viewport combination (36 WCAG scans) with no violations.
- The complete production-browser suite now passes 171/171 in 3.0 minutes with all 87 pixel baselines unchanged.
  Lint/Ruff, all 11 package builds/four SPA outputs and generated OpenAPI drift checks passed at 20:15.
- Closed the explicit Report Studio version-diff gap. Chapter snapshots are now hoisted into one immutable version
  chain shared by editing and comparison; the new URL-addressable “版本对比” workspace selects chapter, base and
  target versions, preserves author/time metadata and renders semantic `<del>`/`<ins>` output plus exact change
  counts without mutating either source snapshot.
- The comparison path starts from honest frozen fixture versions and incorporates newly saved in-session versions.
  A secret-shaped chapter remains impossible to snapshot, so it cannot enter the comparison chain. Selecting the
  same version produces an explicit real-zero difference state rather than implying a failed comparison.
- Initial three-viewport verification caught a metadata selector overriding shared Badge colors at 4.33–4.36:1.
  Narrowing that selector restored the design-system foreground and WCAG AA; the final Report interaction,
  accessibility and eight-workspace visual matrix passes 30/30.
- Added desktop/tablet/mobile version-diff baselines, bringing the versioned visual evidence to 90 images. The
  expanded clean production-browser suite passes 174/174 in 3.1 minutes; lint/Ruff, all 11 builds/four SPA outputs
  and generated OpenAPI drift checks passed at 20:29.
- Hardened the shared identity boundary from key allow-listing to value-level DLP. `tenantPubId`, `tenantLabel`,
  `projectPubId`, `projectLabel`, `userPubId` and `userLabel` are now length-bounded and secret-shape checked before
  the loader result is written into React state; unsafe values become explicit opaque/fallback labels. The same
  projection remains applied at the context provider as defense in depth.
- Expanded all four shared-shell tests from fixture-only context to a validated live-session contract mock whose
  allow-listed project `name` deliberately contains a Cookie assignment, standalone OTP and complete mobile number.
  Across all 12 app/viewport combinations the header/context show only `未命名项目`, the downloaded JSON omits every
  canary, and DOM, URL, localStorage and sessionStorage remain clean.
- The malicious live-projection paths still traverse the project-context, notification and task dialogs, providing
  36 dynamic WCAG scans alongside export parsing and zero console/request-failure assertions. Design-system
  security/Error Boundary tests pass 8/8.
- The clean production-browser suite remains 174/174 in 3.1 minutes with all 90 visual baselines unchanged.
  Lint/Ruff, all 11 builds/four SPA outputs and generated OpenAPI drift checks passed at 20:36.
- Added `scripts/check_frontend_contracts.py` as an executable architecture/contract guard and made it part of
  `pnpm check:api`. It now proves that all four applications retain React 19, React Router Framework Mode build
  wiring, `ssr:false`, the frozen basenames and strict/no-unchecked/exact-optional TypeScript settings.
- The same guard rejects direct `fetch()` and hard-coded `/api/v2/` path literals anywhere in S03-owned application
  code, verifies that `@geo/api-client` is parameterized by the generated `paths` interface, and checks the
  openapi-typescript provenance header. S01's owner-controlled `features/execution` subtree is the only explicit
  exception; its remaining handwritten boundary stays documented as an external ownership gap rather than being
  copied into S03 code.
- The guard also verifies the complete S03 Konva decision record, including the rejected Fabric.js alternative and
  S00/S04 acceptance owner. This preserves the accepted `contracts/adr` ownership boundary while making accidental
  deletion or weakening of the required ADR proposal a failing check.
- The new guard, Ruff, all 11 TypeScript lint tasks, all 11 builds/four Framework SPA outputs and generated OpenAPI
  drift checks passed at 20:39. No runtime source changed after the 174/174 browser run at 20:36.
- Audited every writable non-execution input across the four applications. Customer authorization/profile/assets/
  questions/member schemas all reject secret-shaped values before business state or generated mutations; Report
  chapter versions and comments and Intelligence appeals retain the same fail-closed behavior. No uncovered
  persistent form path was found.
- Strengthened artifact DLP from filename checks to actual byte inspection. Customer E2E now parses the downloaded
  evidence-package JSON and report CSV, validates their required records, and rejects Cookie assignments, bearer/
  token fields, OTP, proxy password, profile path, biometric, full-phone and canary patterns. A hostile secret
  search is also purged from the URL before the package is created.
- Intelligence E2E now downloads and parses the complete manifest after verdict, appeal and second review, verifies
  its exact case/verdict/rule/evidence/time projection and applies the same artifact secret-pattern rejection. All
  three target viewports exercise these real browser downloads.
- Focused Customer/Intelligence artifact runs passed 6/6. The clean complete production-browser suite remains
  174/174 in 3.1 minutes with 90 unchanged visual baselines; lint/Ruff, generated OpenAPI drift and the frontend
  architecture contract guard passed at 20:45.
- Performed a manual visual-quality review of representative Customer and Report desktop/mobile baselines instead
  of treating pixel stability as proof of design quality. The shared hierarchy, action priority, cards, target
  spacing and mobile content containment remain sound; the fixed bottom navigation has reserved content padding
  in the real viewport (its mid-page placement in Playwright full-page images is screenshot compositing behavior).
- The review found Report's first version comparator visually overstated changes: a shared-prefix/suffix algorithm
  marked nearly the whole sentence as replaced when three numeric fragments changed. Replaced it with a bounded
  character-level LCS that groups equal/deleted/added runs, highlights each local edit independently and derives
  exact aggregate deletion/addition counts.
- Report E2E now proves the punctuation replacement and independent-word insertion as separate semantic `<del>` /
  `<ins>` runs, plus the same-version real-zero case. Component tests passed 3/3 and focused three-viewport
  interaction/accessibility/editor+diff visual coverage passed 12/12.
- Regenerated and manually reviewed all three version-diff baselines. The clean complete suite remains 174/174 in
  3.1 minutes; lint/Ruff, all 11 builds/four SPA outputs, generated OpenAPI drift and the frontend architecture
  guard passed at 20:53.
- Added a shared pre-render URL sanitation boundary in `ProductShell`. It removes secret-named parameters,
  secret-shaped values and invalid section identifiers with `replaceState`, while preserving safe filters and the
  browser history state used by feature-level URL controls.
- Added four-application hostile-URL E2E coverage at 1600×1100, 1024×768 and 390×844. Each case enters through a
  multi-kilobyte URL containing a secret-shaped section and `access_token`, proves the default workspace renders
  in under ten seconds, retains a benign parameter, reduces the URL below 300 characters and leaves DOM, storage,
  console and failed requests clean. The original roughly 30 KB probe was rejected by Vite with HTTP 431 before
  application code could run, so the durable browser test uses a still-adversarial roughly 4 KB request that
  reaches the client boundary.
- A focused fresh-server run passed 15/15, including Customer filter restoration through browser back/forward
  history and the new hostile-URL case for all four applications and all three target viewports. This also
  identified an interrupted-run preview process serving the Operations bundle on the Report port; exact orphaned
  workspace preview processes were removed before validation.
- The clean complete production-browser suite passed 186/186 in 3.0 minutes at 21:14. All 11 TypeScript lint tasks,
  Ruff, all 11 builds/four Framework SPA outputs, generated OpenAPI drift and the frontend architecture contract
  guard also passed. The suite continues to assert zero console errors and zero unexpected failed requests.
- All 11 frontend package test tasks passed at 21:15: API client 6, auth 6, design system 8, Customer 9,
  Operations 2, Report 3 and Intelligence 3 executable tests, with the remaining leaf packages explicitly
  accepting no-test status. Added a direct jsdom regression for the shared URL sanitizer that proves secret and
  invalid route values are removed, benign query state and React Router history metadata survive, and a second
  sanitation pass is idempotent; design-system tests then passed 9/9 and its strict typecheck remained green.
- A completion-gate audit found that `@geo/charts`, `@geo/evidence-viewer` and `@geo/workflow-ui` were still passing
  only through `--passWithNoTests`, which was not adequate evidence for the required shared component coverage.
  Added real jsdom tests for ECharts initialization/resize/disposal and its always-present accessible data table,
  including ready/real-zero/insufficient/failed rows; evidence asset/text/bbox binding, semantic history diff and
  explicit missing anchors; and ordered workflow state/detail semantics that do not rely on marker colour.
- Added design-system primitive coverage for all seven non-ready states and their alert/status semantics,
  pagination bounds and emitted page values, dialog Escape handling, and assertive versus polite toast roles.
  The resulting frontend package suite passed all 17 Turbo tasks with 45 executable tests (design system 12,
  charts 1, evidence viewer 2, workflow UI 1, plus the existing client/auth/application tests). All 11 lint tasks,
  Ruff, all 11 builds/four Framework SPA outputs, generated OpenAPI drift and the architecture contract guard
  passed again at 21:20. Production runtime code did not change, so the clean 186/186 browser result remains the
  current E2E evidence.
- Converted the planned-page/visual-baseline relationship from a manually maintained claim into a failing
  architecture check. `check_frontend_contracts.py` now compares the actual navigation against the required
  Customer 9, Operations non-execution 4, Report 8 and Intelligence 7 workspaces; Operations `execution` remains
  an explicit S01-owned external section instead of being duplicated into S03 coverage.
- The guard parses every visual spec and requires its section set to exactly equal the owned navigation set, then
  verifies a checked-in Linux baseline for desktop, tablet and mobile for every declared snapshot. It also proves
  the exact 1600×1100, 1024×768 and 390×844 viewports remain configured for all four products. Customer's pairing
  QR/native-challenge baselines remain additional stage evidence beyond the nine workspace baselines.
- The strengthened generated-contract/architecture guard, all 11 TypeScript lint tasks and Ruff passed at 21:23.
  This change affects only verification code; production sources and the clean 186/186 browser evidence remain
  unchanged.
- Extended the architecture guard from dependency presence to frozen-stack implementation evidence. It now
  requires the shared TanStack Query client/provider; Customer's TanStack Table and React Hook Form + Zod
  resolver usage; ECharts initialization plus its accessible data table; Intelligence React Flow plus the
  accessible propagation relationship table; and Report's PDF.js worker and Konva stage.
- Every application/shared package with a test task must continue to declare Vitest, while root Playwright and
  Testing Library dependencies are guarded. S03-owned runtime code may not import Zustand without first recording
  an ADR that justifies non-derivable client state; the current transitive React Flow dependency is unaffected.
  This makes “Zustand only when necessary” an enforceable boundary rather than an undocumented convention.
- The regenerated OpenAPI/client drift check, expanded frontend contract guard, all 11 TypeScript lint tasks and
  Ruff passed at 21:25. Only verification code changed, so the 45-test component result and 186/186 production
  browser result remain current evidence.
- Re-audited the Customer account authorization and terminal-pairing implementation field by field against the
  custody plan. Owner, responsible operator, custody mode, expiry, region, read/query/draft/publish scopes,
  forbidden actions, revocation right, mask, platform, allowed domain, one-time expiry and the native-platform
  OTP/official QR/Push MFA/passkey/liveness/graphical challenge guidance are all present and actionable.
- Strengthened the main three-viewport lifecycle E2E so a secret-shaped owner first fails validation without
  producing the authorization success state. It then changes and verifies a non-default owner and responsible
  person, customer-device custody, explicit 2026-12-31 expiry, region and all four scopes including publish; the
  pairing confirmation proves `doubao.com`, and completion proves draft/publish are not misrepresented as live
  verified.
- The strengthened account lifecycle passed 3/3 as a focused desktop/tablet/mobile run. The clean complete
  production-browser suite then passed 186/186 in 2.9 minutes at 21:30 with zero unexpected console errors or
  failed requests. All 11 lint tasks, Ruff, generated OpenAPI drift and the full frontend architecture guard also
  passed.

## URLs

- Customer: `http://127.0.0.1:45101/platform/customer/`
- Operations: `http://127.0.0.1:45102/platform/operations/`
- Reports: `http://127.0.0.1:45103/platform/reports/`
- Intelligence: `http://127.0.0.1:45104/platform/intelligence/`

## Evidence

- Typecheck output: Turbo reported `11 successful, 11 total` before the unrelated Python mypy failure.
- Production build output: Turbo reported `11 successful, 11 total`.
- DLP canary evidence: hostile Cookie/token/OTP/proxy password/full phone/profile path/biometric/Authorization
  properties are absent from the serialized safe projection; invalid scopes and admission levels fail closed.
- Customer lifecycle evidence: completion→revocation, refusal and timeout paths all pass in jsdom; tests confirm
  the flow renders no secret input and leaves URL/localStorage/sessionStorage free of canaries.
- Playwright machine evidence: `tests/e2e-results/results.json`.
- Shared-shell action and DLP evidence:
  `tests/e2e/{customer,operations,reports,intelligence}-shared-shell.spec.ts` and
  `tests/e2e/shared-shell-actions.ts`.
- Three-viewport screenshots: `tests/e2e-results/customer-account-{desktop,tablet,mobile}.png`.
- Form screenshots: `tests/e2e-results/customer-forms-{desktop,tablet,mobile}.png`.
- Delivery screenshots: `tests/e2e-results/customer-delivery-{desktop,tablet,mobile}.png`.
- S01 Operations real-API screenshots: `tests/visual-evidence/s01/operations-execution-operations-{desktop,tablet,mobile}.png`.
- Report screenshots: `tests/e2e-results/report-studio-{desktop,tablet,mobile}.png`.
- Intelligence screenshots: `tests/e2e-results/intelligence-workbench-{desktop,tablet,mobile}.png`.
- State screenshots: `tests/e2e-results/state-matrix-{desktop,tablet,mobile}.png`.
- Versioned visual baselines:
  `tests/e2e/{customer,operations,reports,intelligence}-visual.spec.ts-snapshots/` (90 images total: every planned
  Customer, Operations non-execution, Report and Intelligence workspace plus the Customer pairing-QR and native
  challenge stages at all three target viewports).
- URL/history evidence covers `section`, `window`, `model`, `mode` and `region`; invalid values fail to defaults.
- Hostile URL evidence is implemented by `tests/e2e/shared-hostile-url.ts` and all four shared-shell specs; it
  covers the secret parameter/value purge, benign-query retention, bounded final URL and clean browser surfaces.
- Report, Intelligence and state-matrix Playwright screenshots are captured.
- Real identity/project evidence: HTTP 200 from `/api/v2/identity/session` and `/api/v2/projects?limit=1` at the
  local real API using safe header context only; the returned project display name was `S01 E2E Project`.

## Risks

- Customer/Report/Intelligence product records are contract fixtures, not live business data; S02's routers remain
  unmounted in the shared API/OpenAPI and the UI must retain explicit fixture labelling.
- The current S01 account contract has no customer-safe projection. S01 documents that customer roles cannot read
  account/profile projections, so the customer account lifecycle cannot yet be honestly switched to the real API.
- A read-only 18:52 database audit found no currently active customer membership in a tenant that also has a
  platform account, so the stronger valid-customer existing-vs-missing real-API comparison cannot be run without
  creating backend fixture state. S03 did not write or fabricate that external fixture.
- No live external account is available. Admission must remain at or below its evidenced level.
- The workspace `.git` directory lacks repository metadata; Turbo cannot derive a Git dirty hash.
- Whole-repository format/legacy guards remain red on the two S02-owned formatting files and a legacy
  `server/geosys/app.py` hash mismatch. S03 does not have authority to rewrite those targets.
- The 21:31 live-OpenAPI re-audit still shows 36 paths and zero analytics/evidence/exports/reports/intelligence
  paths. Identity, projects, generic project resources, customer,
  collection and governance routers are mounted, but the independent S02 analytics/evidence/exports/reports/
  intelligence router remains absent from `api/geo_platform/main.py` and none of those product paths appears in the
  live schema.

## Next action

- Resume when the S04-owned shared application mounts S02's analytics/evidence/exports/reports/intelligence router,
  S01 provides the frozen customer-safe account projection, and an approved external-platform qualification target
  is available. Regenerate the client, replace the explicitly labelled fixtures incrementally, rerun role/DLP
  negatives and the complete 45-test/186-E2E/three-viewport gate, then reassess all seven completion gates.

## Blocked audit — 2026-07-24 21:33

- This is the fifth consecutive live-schema recheck in the current goal run with the same result: 36 mounted paths
  and zero analytics/evidence/exports/reports/intelligence paths. `api/geo_platform/s02_routers.py` exists, but
  `api/geo_platform/main.py` still does not import or mount it; that shared-main integration is outside S03
  ownership.
- The frozen S01 contract still deliberately denies customer roles access to platform-account/profile projections
  and exposes no customer-safe account summary/pairing/events/revocation family. S03 cannot safely infer or
  duplicate that S01-owned contract.
- S01's current status still records that no real external platform credential is available and that the only
  executable adapter must remain `adapter_ready`. Therefore the required platform-native
  login/read/draft/publish qualification cannot be truthfully performed.
- All independently actionable S03 work is exhausted: four applications and every owned workspace are implemented;
  navigation-to-visual coverage is guarded; 45 frontend component tests pass; the current Playwright result records
  186/186 passed with 90 baselines and no unexpected console/network failures; strict lint, Ruff, production builds,
  generated-client drift and the expanded frontend contract guard pass.
- Because the same external condition has persisted for more than the required three rounds and no remaining
  S03-owned page or safe contract replacement is available, the persistent goal is now correctly marked blocked,
  not complete. Fixtures remain visibly labelled and admission claims remain conservative.
