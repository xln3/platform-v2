# S04 — Integration, migration and production certification

Updated: 2026-07-27 12:45 (Asia/Shanghai)
Status: in progress — source-owned production backlog is closed; six external authority/sample gates remain

## Audit baseline

- Created the persistent S04 goal without a token budget.
- Re-read the implementation plan, implementation status, comparison report, account/session custody plan,
  S00–S03 status, accepted ADRs, contract gaps, conventions, migration allocation and current OpenAPI structure.
- Did not accept prior completion claims as evidence. Initial source inspection found 44 S01/platform paths and no
  mounted S02 paths; the four applications still contain labelled contract fixtures.
- Current real dependencies are running and healthy for PostgreSQL/pgvector, ClickHouse, Temporal, Temporal UI,
  MinIO and Redis. API and browser processes require fresh post-integration verification.
- S03's older customer-safe contract blocker is superseded by S01-002 but its frontend replacement remains undone.

## In progress

- Mounted the additive S02 analytics/evidence/exports/reports/intelligence router bundle in the shared FastAPI
  application; regenerated a 69-path OpenAPI/client and passed drift/frontend architecture guards.
- Promoted the fully specified Konva proposal to accepted ADR-0005.
- Building a code- and evidence-backed gap ledger mapped to plan sections 17/18 and AS-01–AS-10.
- Added and applied `s04_merge_0001`, merging the S01/S02 Alembic heads, followed by `s04_0002`
  to align real audit actor/action widths with the API/idempotency contract.
- Fixed the global stable error envelope and request-ID propagation. Validation details contain
  field location/type but never rejected input values.
- Disabled the development identity bootstrap outside development/test and moved its secret to configuration.
- Ran the integrated API against real PostgreSQL and found/fixed the audit-width 500. Evidence:
  `tests/s04-evidence/integrated-api-runtime.json`.
- Ran EvidenceCaptureWorkflow through the real S01 capability endpoint into MinIO/PostgreSQL;
  explicit lease revocation blocked the next capture. Evidence:
  `tests/s04-evidence/real-gateway-evidence-runtime.json`.
- Added a tested metadata-only legacy sensitive inventory generator. Current evidence contains
  237 candidates, 8,545,825,763 bytes, hashes and classifications but no raw path or secret:
  `tests/s04-evidence/legacy-sensitive-inventory.json`.
- Created a read-only, integrity-checked SQLite legacy snapshot and a source-to-target mapping; added durable
  migration-run, ID-map and watermark control tables plus append-only V2 raw-answer truth.
- Implemented the same-input shadow scorer and a fail-closed reconciliation writer covering task matrix, answers,
  eligibility, citations, KPI, reports and evidence. The first real run exposed 629 unapproved differences.
  Fixes aligned cooperative risk-control account eligibility, unmentioned-brand sentiment, citation ordinal,
  own-domain and URL-normalizer contracts. The repeated run has 0 differences and 0 approvals:
  `tests/s04-evidence/shadow-scoring/reconciliation.json` and `reconciliation.md`.
- Executed a resumable core migration into real PostgreSQL: identity metadata (never passwords/sessions), three
  customers, five projects/config snapshots, five project-scoped brands, three project-scoped competitor rows,
  77 queries, seven historical collection runs, 192 historical tasks and 146 append-only raw answers. A full
  repeat wrote zero new mappings/rows: `tests/s04-evidence/core-migration-history-rerun.json`.
- Rebuilt 146 analytics rows and 2,754 citations with the V2 scorer rather than copying derived legacy values.
- Verified old CAS hashes, applied DLP, uploaded 533 admissible screenshot/share/SSE assets to MinIO and verified
  the target bytes. A repeat uploaded zero objects. 149 HAR and six captcha objects remain quarantined under the
  documented sensitive-artifact policy: `tests/s04-evidence/evidence-migration-run.json` and
  `evidence-migration-rerun.json`.
- Reconciled the actual persisted PostgreSQL/MinIO target against legacy: task matrix, scored answers,
  146 eligibility facts, 539 citations, KPI and 533 admitted evidence rows all have zero differences and zero
  approvals: `tests/s04-evidence/migrated-target-reconciliation.json` and `.md`.
- Proved interruption recovery by injecting a stop after the collection-history phase and resuming from durable
  watermarks; the recovery and subsequent full rerun wrote zero duplicate rows.
- Projected all 146 migrated answer events through the real outbox consumer into ClickHouse; 146 answer IDs and
  146 event IDs are unique and no migration outbox row remains unpublished.
- Removed the production collection fixture: an unconfigured adapter now fails closed and the deterministic
  adapter exists only in test code. Production frontend authentication can no longer fall back to contract
  identities; fixture-heavy workspaces are not represented as live.
- Audited the live production topology without reading secrets. The old `geosys.service` and all old Nginx routes
  remain active and unchanged. A fresh restricted online backup has SQLite integrity `ok`, a SHA-256 manifest,
  738 CAS entries and a metadata-only inventory of 13,063 sensitive profile files:
  `tests/s04-evidence/production-topology-predeploy.{json,md}`.
- Deployed the production-isolated PostgreSQL, ClickHouse, Temporal, MinIO and Redis compose stack on distinct
  loopback ports and persistent volumes. Installed and enabled the API, collection worker, S02 worker and analytics
  outbox worker as hardened systemd services; all dependency health checks and all four units are active.
- Applied the Alembic chain through `s04_0005` to the actual production DSN after fixing Alembic's prior
  development-DSN override. Ran the controlled migration from the immutable predeployment SQLite backup, then
  reran core and evidence migration to prove idempotency. Production target reconciliation has zero approved or
  unapproved differences: `tests/s04-evidence/production-core-migration.json`,
  `production-core-migration-rerun.json`, `production-evidence-migration.json`,
  `production-evidence-migration-rerun.json` and `production-target-reconciliation.{json,md}`.
- Installed additive `/platform/*` and `/api/v2/*` Nginx routes without replacing any legacy location. Direct
  no-proxy HTTPS probes prove all four application entries, their scoped JavaScript assets, `/api/v2/health`,
  `/portal`, `/ops`, `/client` and `/api/health` return 200 without legacy redirects. The legacy service
  remains active.
- Found and fixed two production-only ClickHouse gaps: wildcard binding conflicted on this host and no persistent
  outbox process existed. Added an IPv4 listen override and a credentialed, idempotent analytics worker. It
  projected all 146 answer events into 146 answer facts, 2,754 citation facts, 146 run events and 1,022 metric
  rows. A forced duplicate delivery left both the receipt and answer counts unchanged:
  `tests/s04-evidence/production-runtime-data-counts.json`.
- A driver failure initially embedded a PostgreSQL DSN in the local service journal. Logging now emits only the
  exception class, the affected database credential was immediately rotated, and all four V2 services were
  restarted successfully with the replacement.
- Fixed Playwright web-server startup to use polling under the host's exhausted inotify allocation.
- Found and fixed a production-only static deployment defect: all four applications emitted colliding root
  `/assets/*` URLs. Each React Router basename and Vite base now uses an application-scoped
  `/platform/<application>/` prefix, including runtime-preloaded dynamic chunks. The frontend contract guard
  enforces that invariant while preserving the standard build command.
- Made contract identities test-explicit via `VITE_ALLOW_CONTRACT_FIXTURES=true`; an ordinary production build
  cannot silently fall back to a fixture. Test builds no longer make a deliberate failing identity request before
  selecting their labelled fixture.
- Restored the already-real customer question/change-request workspace for live sessions. Its E2E proves validated
  identity headers, DLP rejection, idempotency key and generated OpenAPI write behavior.
- Ran the complete four-application desktop/tablet/mobile browser matrix against the current source and current
  API: 189/189 passed in 189,831 ms, with zero skipped, unexpected or flaky tests. Coverage includes WCAG,
  cross-role isolation, secret surfaces, hostile URLs, lifecycle calls, full-page visual baselines and the
  customer/report/intelligence workflows. Machine-readable evidence:
  `tests/s04-evidence/e2e-results.json`.
- Restarted the stale integration API before certification. The current health-check event proves
  `L0=passed` and `L1–L4=not_run`; old `fixture` level values are not accepted as current evidence.
- Re-ran lint, Python ruff, formatting, strict TypeScript, mypy (82 source files), OpenAPI generation/drift,
  frontend contract guard, all 11 builds and the legacy-route guard successfully.
- Corrected migrated public IDs to the Crockford alphabet and normalized legacy roles into the supported V2
  role set. After a scoped V2-only rebuild from the immutable legacy backup, production advanced through
  `s04_0007`,
  has no invalid public IDs, and target reconciliation remains at zero differences.
- Re-audited the earlier browser evidence and rejected its synthetic role claims: browser-controlled
  `X-Actor-*` values were application authorization tests, not five real human production identities. Production
  now validates the legacy HttpOnly session read-only, maps its source user/tenant through the migration ID map,
  and derives the role from the current V2 membership. Forged actor headers without a valid cookie return 401.
  The current real-session matrix is 15/15 (four application entries plus the Operations execution deep link,
  each at desktop/tablet/mobile), with the only available
  production human role `admin`; all checks have zero console/page/request/HTTP errors:
  `tests/s04-evidence/production-browser-acceptance.json`, `production-identity-certification.json` and
  `production-screenshots/`.
- Created a restricted certified V2 backup and restored PostgreSQL, ClickHouse and MinIO independently into
  temporary targets. Schema/data counts and all backup hashes matched, and temporary restore targets were
  removed: `tests/s04-evidence/production-backup-restore.{json,md}`.
- Benchmarked the production API with 1,000 requests at concurrency 25 per endpoint. Health sustained
  494.989 req/s at 131.853 ms p95; authenticated projects sustained 423.007 req/s at 86.089 ms p95, with
  2,000/2,000 successful responses: `tests/s04-evidence/production-api-performance.json`.
- Deployed loopback-only OTel Collector, Prometheus, Loki, Alloy and Grafana. Prometheus has three healthy
  targets, journal logs are queryable in Loki, and the Grafana production dashboard is provisioned. A single
  production trace proves API → Temporal workflow → Activity propagation. The first workflow probe exposed
  and fixed a real migration gap: executable V2 config snapshots now include `models` and `query_groups`,
  and the repeated request returned 202 and completed:
  `tests/s04-evidence/production-observability.{json,md}`.
- Fixed `GeoCollectionWorkflow.status` from the unserializable `dict[str, object]` query annotation to a
  concrete dataclass after a real production Temporal client exposed the defect. Production certification now
  proves retry after Activity failure, a heartbeating long Activity, duplicate Signals, cancellation while the
  Worker is stopped, Worker restart and durable replay. No certification workflow remains running:
  `tests/s04-evidence/production-workflow-resilience.{json,md}`.
- Ran 60 focused security regressions covering tenant scope, role downgrade, stable secret-free errors, Vault
  AEAD/rekey/cryptographic deletion, DLP, fenced and capability leases, customer pairing, revocation, break-glass,
  signed evidence access, audit, MinIO integrity and ClickHouse secret rejection. All 60 passed:
  `tests/s04-evidence/security-regression.json`.
- Fixed collection-run lookup to scope its project/config maps to the principal tenant instead of loading global
  catalog rows.
- Certified bounded capacity using disposable non-customer data: one million PostgreSQL rows, one million
  ClickHouse rows, a hash-verified 32 MiB MinIO object and a 15-second heartbeating Temporal Activity. Temporary
  structures and the test object were removed: `tests/s04-evidence/production-capacity.{json,md}`.
- The latest complete quality gate passes: 97 Python tests, all 11 TypeScript package tests/typechecks/builds,
  Ruff/Prettier/mypy (86 source files), OpenAPI regeneration/drift, frontend contract and legacy-route guards.
  The complete browser matrix is now 216/216 with zero skipped, unexpected or flaky tests.
- Found and removed a release-process mock leak: Playwright fixture builds previously wrote into the same
  `build/` directory Nginx serves. E2E now writes only to `build-e2e/`, preview ports are strict, and production
  roots omit the fixture object entirely. An automated post-build guard proves no contract identity payload is
  shipped. The rebuilt production applications pass 15/15 real-session route/viewport checks with zero console, page,
  request or HTTP errors: `tests/s04-evidence/full-quality-certification.json`.
- Audited all 28 production application workspaces after removing misleading live-state sample aggregates.
  Customer membership now rejects non-admin sessions before making an admin-only request. The repeated scan
  has 28/28 HTTP 200 entries, zero fixture/mock markers and zero console, page or request failures:
  `tests/s04-evidence/production-mock-scan.json`. The repeated real-session acceptance remains 15/15.
- Removed the final production browser-controlled identity path from Operations execution. Its deep route now
  consumes the shared validated session, uses same-origin cookie authentication in production, clears stale
  actor hints, no longer defaults API traffic to the development port, and uses the generated OpenAPI client for
  every lifecycle read/control call. A header merge defect found by the migrated E2E was fixed; the production
  matrix verifies no `X-Actor-*` request is emitted.
- The renewed full package gate exposed missing Router harnesses in Report Studio and Intelligence component
  tests; both were fixed. A hostile report cursor also left a coupled page number behind after DLP removal.
  URL sanitization now atomically removes secret-shaped cursors and their page indices, with a regression test
  and inspected desktop/tablet/mobile visual baselines.
- Made production profile custody fail closed. `LocalKms` remains available only to development/tests because
  its process-configured wrapping key cannot be an independently retained deletion authority across database
  restores. Before Vault Transit was configured, production profile seal/open operations returned the stable
  `profile_vault_unavailable` error rather than creating recoverable local wrapped DEKs.
- The final route probe caught and removed an invalid production cutover that redirected legacy pages. The
  deployed Nginx server is now the hash-verified predeployment configuration plus exactly one additive V2
  include. `/portal`, `/ops` and `/client` return 200 without redirects, `/geo` retains its legacy login
  boundary, and both legacy/V2 health routes return 200:
  `tests/s04-evidence/production-route-coexistence.json`.
- Closed the database-owner RLS bypass found during the final audit. Migration `s04_0007` enables and forces
  tenant policies on all 73 platform/analytics/evidence/reporting/intelligence tenant tables. The production
  API now uses a dedicated `NOSUPERUSER/NOBYPASSRLS` role, while every SQLAlchemy and psycopg request sets
  transaction-local UUID/public-ID tenant selectors before business queries. A populated two-tenant PostgreSQL
  test passes; production (which has one tenant) proves no-context reads return zero, own-tenant results match
  owner truth and `row_security=off` is rejected. The schema change was preceded by a restricted, PostgreSQL-16
  validated backup: `tests/s04-evidence/production-rls-certification.json`.
- Split database execution identities so a Worker restart cannot accidentally inherit the API role or database
  owner. Main/S02/outbox Workers now use a dedicated `NOSUPERUSER/NOBYPASSRLS` role; collection Activity
  contracts carry the tenant selector and S02 direct connections set it transaction-locally. Migration
  ownership remains separate. All four services were restarted successfully with the new credentials.
- Implemented the formerly open project-scoped customer declaration contract. Migration `s04_0008` adds
  forced-RLS version tables for customer profiles and atomic asset confirmations. Customer-role writes require
  an explicit truth declaration and idempotency key; replay conflicts fail closed, histories are cursor-paged,
  cross-tenant access is indistinguishable from absence, DLP rejects secret-bearing fields, and actor subjects
  are retained for audit but omitted from customer responses. The asset transaction creates the confirmation
  version and its brand/competitor catalog projections together. Customer Web now reads and writes both
  contracts through generated OpenAPI paths instead of a fixture or an insufficient-state placeholder.
  Focused integration tests pass 2/2, the complete Python suite passes 99/99, Customer Web unit tests pass
  11/11, and the desktop/tablet/mobile live E2E exercises both writes. The final complete four-application
  matrix passes 216/216 in 213,514.907 ms with zero unexpected, skipped or flaky tests. A restricted
  PostgreSQL-16-validated
  predeployment backup preceded the production migration to `s04_0008`; all 75 tenant tables remain
  ENABLE+FORCE RLS. The rebuilt Customer Web profile and asset workspaces pass desktop/tablet/mobile production
  checks with zero console/page/request/HTTP errors. No real customer declaration was fabricated to prove a
  write: production write semantics are covered by the populated integration and browser contract tests.
  Evidence: `tests/s04-evidence/production-customer-confirmations.json`,
  `production-browser-acceptance.json`, `production-rls-certification.json`, and
  `production-screenshots/customer-{profile,assets}-{desktop,tablet,mobile}.png`.
- Closed the responsible-person contract drift in source. Customer-safe account registration and authorization
  now accept an independently selected active tenant member. The generated member projection excludes
  disabled/revoked/service identities and returns only opaque IDs, roles and non-PII labels; cross-tenant and
  missing assignments share one 404. Customer Web uses the generated list and sends the selected ID in both
  writes instead of accepting a free-form name that the backend silently discarded. Focused PostgreSQL
  integration tests and desktop/tablet/mobile live contract E2E pass. The rebuilt production Customer account
  workspace passes all three viewports, and the expanded real-session matrix passes 24/24 with zero
  console/page/request/HTTP errors. Evidence:
  `tests/s04-evidence/production-responsible-member-assignment.json`,
  `production-browser-acceptance.json`, and
  `production-screenshots/customer-accounts-{desktop,tablet,mobile}.png`.
- Implemented and production-qualified the remaining Customer analytics detail reads. A tenant-scoped `/analytics/breakdown`
  contract now returns bounded daily, model, region+mode and question aggregates from eligible answers and their
  latest analyses without account/profile dimensions. It validates the date window, uses fixed SQL group
  expressions, strips secret-shaped dimension labels, and distinguishes zero/empty/insufficient values.
  Customer Monitoring consumes all four generated views for the previously unsupported model chart, true daily
  trend, region/mode table and question rows. The full gate passes 99/99 Python tests plus TypeScript, OpenAPI
  regeneration, four production builds and bundle/legacy-route guards. The analytics production dataset is
  unchanged under current schema `s04_0009`;
  its rebuilt 146 answers/146 analyses/2,754 citations yield 3 day, 2 model, 3 region-mode and 76 question
  dimensions. The expanded real-session browser matrix passes 30/30, including Monitoring at desktop/tablet/
  mobile with zero console, page, failed-request or HTTP-error evidence. Evidence:
  `tests/s04-evidence/production-analytics-breakdowns.json`,
  `production-browser-acceptance.json`, and
  `production-screenshots/customer-monitoring-{desktop,tablet,mobile}.png`.
- Implemented and deployed typed report-detail contracts. `s04_0009` adds tenant-FORCE-RLS immutable frozen
  fact rows and append-only effect retests; detail reads include components, fact hashes, artifacts, reviews,
  comment history, events, actions and retests. Artifact access verifies stored SHA-256 before returning a
  tenant-scoped, no-store response. Report Studio consumes the generated projections and performs real comment,
  review, publish, action-state, retest and outcome writes. PostgreSQL/MinIO integration and Report Studio
  desktop/tablet/mobile live E2E pass 6/6; the full gate remains 99/99 Python plus all TypeScript/build/contract
  guards. A restricted PostgreSQL-16-validated backup preceded production migration to `s04_0009`. Production
  report rows are zero, so no customer report or approval was fabricated: production route/schema/empty-state
  acceptance passes while populated production report acceptance remains explicitly open. Evidence:
  `tests/s04-evidence/production-report-detail-contracts.json`.
- Implemented and deployed Intelligence page-history and visual/text-diff reads. Evidence-backed content ingestion
  records an immutable snapshot for every version and a hash-bound diff for adjacent evidence versions. Two
  generated tenant-scoped endpoints expose safe history/diff projections without object keys or diff body text;
  Intelligence Web renders real version, source host, time, hash, similarity and visual-availability state.
  PostgreSQL integration and desktop/tablet/mobile live + visual E2E pass 6/6, including hostile diff text not
  entering the DOM. Production contains zero investigations/content versions/snapshots/diffs, so the expanded
  30/30 browser matrix certifies route and empty-state behavior without fabricating an investigation; populated
  production history remains open. Evidence: `tests/s04-evidence/production-intelligence-history.json`.
- Implemented and deployed answer-scoped citation/evidence/anchor/history relations. The generated endpoint joins
  only tenant-owned answer facts and linked, non-deleted evidence; URL query/fragment, object keys, diff bodies and
  unknown bbox fields are not projected. Customer Web now loads the selected answer's real relations instead of
  showing the tenant-wide evidence inventory in its dialog. PostgreSQL integration, 99/99 Python, the complete
  static/contract/build gate and desktop/tablet/mobile hostile-payload E2E pass. Production was rebuilt/restarted
  and the real-session matrix remains 30/30 with zero browser errors; the mapped customer workspace has zero answer
  cards, so populated production relation acceptance remains open rather than fabricating an answer. Evidence:
  `tests/s04-evidence/production-answer-evidence-relations.json`.
- Implemented and deployed report fact-to-evidence binding projections. Report detail now joins each immutable
  binding to its tenant-owned evidence metadata and anchor count while excluding object keys and source URLs;
  Report Studio renders the frozen binding table from the generated contract. PostgreSQL integration and hostile
  payload desktop/tablet/mobile E2E pass. The final isolated E2E server snapshots each build and serves `.mjs`
  workers with JavaScript MIME, preventing concurrent rebuilds from invalidating certification; the full matrix
  passes 216/216 in 213,962.665 ms. The final gate passes 99/99 Python and all TypeScript/OpenAPI/build/bundle/
  legacy-route guards. Production was rebuilt without fixture identities, API `0.1.0-s04` restarted, and the
  real-session browser matrix passes 30/30 with zero browser errors; `/geo` remains 401 and was not switched.
  Production has zero report rows, so populated binding acceptance remains open without fabricated data.
  Evidence: `tests/s04-evidence/production-report-evidence-bindings.json`,
  `tests/s04-evidence/e2e-results.json`, and `tests/s04-evidence/production-browser-acceptance.json`.
- Corrected the production-only PDF worker MIME gap that empty report data did not exercise. The additive V2
  Nginx snippet now scopes `.mjs` JavaScript responses to Customer Web and Report Studio only; its prior version
  was backed up before `nginx -t` and reload. Both workers return `application/javascript`, all four V2 SPAs and
  health return 200, `/geo` remains 401, and the post-reload production browser matrix passes 30/30 again.
  Evidence: `tests/s04-evidence/production-mjs-mime.json`.
- Added the previously missing external deletion-authority adapter without weakening the production fail-closed
  boundary. Vault Transit configuration requires a clean HTTPS origin, rejects URL credentials/paths/queries,
  reads a non-symlinked owner-only token from a systemd credential file on every operation, passes profile AAD as
  Transit context, validates opaque ciphertext/32-byte DEK responses and maps transport/response failures to a
  secret-free unavailable error. Each tenant+account maps to a separate non-identifying Transit key, so deleting
  that key invalidates wrapped DEKs retained in database backups. Runtime, provisioning and deletion ACLs are
  separate; the API runtime policy cannot create, configure, export, back up or delete keys. Account-key deletion
  is idempotent and all non-404 failures fail closed. The production revocation Activity now destroys the external
  account key before committing profile purge completion; a later database failure is safe because Temporal retry
  accepts the already-missing key. A profile-bearing production revocation fails closed when its separately
  mounted deletion credential is absent, while zero-profile revocations do not require a key. Deployment
  templates contain no token. Focused security tests pass 17/17 and the full Python suite passes 111/111.
  The later production deployment below supersedes the original unconfigured state; authorized per-account
  import/cutover and independent organizational custody remain open. Evidence:
  `tests/s04-evidence/external-kms-adapter.json`.
- Verified the deletion mechanism against an isolated real HashiCorp Vault 2.0.3 dependency over a temporary TLS
  CA, not only a mock transport. Two synthetic accounts received distinct Transit keys and both profiles opened;
  after deleting account A's key and recreating the same key name with fresh material, the retained account A
  backup ciphertext/wrapped-DEK remained undecryptable while account B still opened. The container, root token
  and temporary private keys were removed, and no production or legacy profile was touched. This proves the
  mechanism and backup non-reactivation invariant, but not production authority provisioning or an authorized
  customer-profile deletion. Evidence: `tests/s04-evidence/vault-transit-real-integration.json`.
- Hardened production browser certification against API restart races. A post-deploy run correctly exposed one
  transient identity 502 when it began during Worker replacement; the tool now requires three consecutive health
  200 responses before opening a browser. A deliberate API restart followed by the hardened gate passes 30/30
  with zero console/page/request/HTTP errors, so transient startup failures can no longer be silently recorded as
  release evidence.
- Made the external-gate audit reproducible instead of relying on a hand-maintained status file. The new audit
  reads the protected production environment without emitting values, counts account/profile/DEK/adapter/current
  authorization state directly from PostgreSQL, and joins only non-secret topology and identity evidence. Its
  latest run confirms zero production accounts, profiles, active DEKs, adapters and current authorizations; only
  the real `admin` role is verified, production Vault mechanics are configured, and all 13,063 legacy candidates
  remain metadata-only. Evidence: `tools/external_gate_audit.py` and
  `tests/s04-evidence/external-gates-audit.json`.
- Implemented and deployed the final OIDC resource-server boundary without pretending that a production IdP
  exists. The verifier requires a clean HTTPS issuer/JWKS URL, fixed asymmetric algorithm allowlist, `at+jwt`,
  issuer, audience, expiry, issue time, subject, tenant claim and a bounded token lifetime; HS/JWK algorithm
  confusion and browser actor/role spoofing fail closed. Issuer and subject are stored only as SHA-256 identity
  bindings, while the active tenant membership remains the sole role authority. `s04_0010` adds the empty
  tenant-scoped binding table with RLS and FORCE RLS. A restricted 1,791,733-byte PG16 custom backup
  (`47adabb3…51591c`, 803 catalog entries) preceded the production migration; a repeated upgrade was a no-op.
  Production now reports 78/78 tenant tables with RLS/FORCE RLS, API/Worker are healthy, and the post-deploy
  browser matrix passes 30/30. Production deliberately remains `legacy_session` because no IdP/JWKS or real
  customer/operator/analyst/reviewer bindings are available. Evidence:
  `tests/s04-evidence/oidc-identity-boundary.json` and
  `tests/s04-evidence/production-rls-certification.json`.
- Completed the source-owned browser identity and binding lifecycle on top of that verifier. Login now uses
  OIDC `form_post` authorization code with S256 PKCE, an AES-256-GCM state/verifier cookie that expires after
  five minutes, issuer-origin-pinned authorization/token endpoints and a bounded HttpOnly/Secure/SameSite access
  cookie. Authorization codes do not enter callback URLs, refresh tokens are never retained, simultaneous Bearer
  and cookie credentials are rejected, and logout clears both cookies. Admins can list, create and revoke
  per-member OIDC bindings through generated contracts and Customer Web; only normalized issuer/subject hashes
  are stored, raw subjects are cleared from the form and never enter responses or audit receipts. End-to-end
  testing exposed and fixed two integration defects: OIDC principals used a different internal user identifier
  than `/identity/session`, and the production members workspace incorrectly required a browser-controlled role
  header that the secure cookie mode intentionally removes. It also exposed a duplicate `--force` in the E2E
  launcher; the full matrix was rerun rather than restoring overwritten evidence. Current gates pass 144/144
  Python, 216/216 three-viewport E2E, 83-path OpenAPI regeneration, all TypeScript/build guards, and 33/33
  production browser checks including the members workspace at desktop/tablet/mobile with no actor headers or
  runtime errors. Production remains `legacy_session` with zero binding rows because no real IdP/client
  registration or missing-role identities were supplied. Evidence:
  `tests/s04-evidence/oidc-identity-boundary.json`,
  `tests/s04-evidence/e2e-results.json`, and
  `tests/s04-evidence/production-browser-acceptance.json`.
- Closed the source-owned portion of AS-05 that the earlier pairing UI did not prove. Customer-device
  interventions now require an Ed25519 device-key possession proof before a one-time pairing capability is
  consumed. The API issues a five-minute, nonce-bearing, account/device/domain/action/challenge-scoped task
  signed by an independent Ed25519 production credential; the terminal result is signed and bound to the exact
  task payload hash. A terminal may only report `challenge_completed`; the account remains paused until a
  separately authenticated platform callback or identity probe attests the result. Forged proofs, token reuse,
  result replay and completion after account/device revocation
  fail closed, and ordinary operator completion can no longer mark a `customer_device` intervention verified.
  `s04_0011` adds tenant-scoped device bindings and terminal tasks with RLS/FORCE RLS. A 1,795,897-byte PG16
  backup (`de61eaf4…21c953`, 812 catalog entries) preceded the production migration; an independently validated
  1,805,176-byte post-migration backup (`6c196772…f783d`, 832 entries) followed it. Production now reports
  80/80 tenant tables with RLS/FORCE RLS, 86 OpenAPI paths, 144/144 Python tests, mypy across 91 modules,
  33/33 browser checks and 28/28 runtime mock checks. Production contains no device/task rows because no real
  customer terminal was fabricated. Native OTP/QR/Push/passkey/face/graphical canaries and a real Agent or
  extension remain an external gate. Evidence: `tests/s04-evidence/customer-terminal-protocol.json`.
- Hardened the AS-05 timeout and workflow edges found in the follow-up audit. Expired pairing capabilities and
  issued terminal tasks now atomically enter an auditable `expired` terminal state instead of returning 410
  while remaining stuck in `paired`/`issued`. A terminal platform attestation for a linked collection run now
  resumes the durable workflow only after the attestation commit. `HumanInterventionWorkflow` is first-result
  immutable, so a later conflicting nonce cannot overwrite the authoritative outcome. Focused API/Temporal tests
  pass 15/15, the full suite remains 144/144, and the post-restart production browser matrix remains 33/33.
- Hardened the §17.6 Anti-GEO evaluation boundary without claiming calibration from a synthetic fixture. Every
  evaluation now requires explicit dataset and scorer versions, binds predicted labels to a declared threshold,
  rejects out-of-range probabilities and duplicate propagation clusters, and emits a reproducible dataset hash,
  class counts, precision, recall, false-positive rate, Brier score, expected calibration error and explanation
  completeness. Negative tests prove the gameable inputs fail closed; the machine artifact explicitly records
  that no approved external label set or production model admission exists. The full suite is now 144/144.
  Evidence: `tests/s04-evidence/anti-geo-evaluation-boundary.json`.
- Added a reproducible §18 unified completion audit that composes and hashes the current quality, browser,
  identity, RLS, migration reconciliation, runtime-count, terminal and Anti-GEO artifacts. Seven of ten
  operational evidence conditions currently pass; implementation/configuration completeness, integration with
  the missing real external dependencies, and five-role real operation do not. It records six remaining
  requirement groups and keeps the goal active rather than converting absent authority or empty source samples
  into positive evidence. Evidence: `tests/s04-evidence/unified-completion-audit.json`.
- Implemented and additively deployed the restricted MV3 customer-terminal extension instead of reusing the
  legacy session-export extension. Its Ed25519 device private key is a non-extractable IndexedDB `CryptoKey`;
  it pins the server signing-key fingerprint, verifies signed tenant/account/device/domain/action/challenge
  task scope and expiry, requests only the exact runtime host origin, and can submit only a signed
  `challenge_completed` receipt. The build rejects cookie, proxy, page-content, OTP and biometric APIs. The
  Operations customer-device path now emits the one-time scoped pairing bundle and never offers the direct
  operator-completion action. Protocol tests pass 2/2, Operations tests pass 3/3, the full Python suite passes
  144/144, OpenAPI regeneration has no drift, and the production manifest is served at
  `/platform/customer-terminal-extension/manifest.json`. Nginx and all four V2 services are active; legacy
  `/geo` remains unchanged. Evidence:
  `tests/s04-evidence/customer-terminal-protocol.json`.
- Added a reproducible headful Chromium/Xvfb runtime certification for the restricted MV3 extension. It drives
  the real browser optional-origin permission prompt, binds a signed task from a real local PostgreSQL-backed API,
  proves the IndexedDB Ed25519 private key is non-extractable and rejects export, verifies the same public-key
  fingerprint survives a full browser restart, and submits the signed minimal completion. The backend remains
  `awaiting_platform_probe`, proving the extension cannot self-attest platform verification. This is a local
  fixture and does not close the authorized customer/native-platform gate. Evidence:
  `tests/s04-evidence/customer-terminal-extension-runtime.json`.
- Replaced the unsigned-directory-only release boundary with a stable-ID CRX3 package while retaining the
  unpacked artifact for audit. Production serves the 7,678-byte CRX at
  `/platform/customer-terminal-extension/customer-terminal-extension.crx` with SHA-256
  `a0377861ced4edc8ab2a8199e2b09cb459e312c4ea087080d75f3311ecc7c7be`; certification verifies the CRX3
  header and RSA/SHA-256 payload signature, private/public key correspondence, public-key-derived extension ID
  `ikgoabapccnkocienglhebfnefaibdoj`, production bytes and release metadata. The private signing key is
  root-only mode `0600`; its 10,240-byte root-only backup is
  `.production-backups/20260725T020500Z/customer-terminal-extension-signing.tar` with SHA-256
  `e7f27c57ed7c5c3a3b49a01271e2925a0cbb890c165d71288c3fc12cdf47174f`.
  Signing does not constitute customer installation or a native-platform canary. Evidence:
  `tests/s04-evidence/customer-terminal-extension-release.json`.
- Audited the CI definition rather than treating its presence as proof. The workflow was structurally broken:
  this repository root is already Platform V2, while every job used `working-directory: platform-v2` and its
  triggers only matched `platform-v2/**`. Replaced it with five root-level jobs: fresh pgvector Compose plus
  Alembic and Python integration, TypeScript lint/type/test/build, OpenAPI/production-bundle/legacy-route/CI
  guards, 216-case Chromium E2E with retained failure artifacts, and real-dependency smoke. The smoke path now
  always uses the pgvector overlay and checks that the vector extension is available; this also prevents silently
  replacing the local pgvector container with plain PostgreSQL. The full local equivalent passes: 144/144 Python,
  12 packages, 216/216 E2E and smoke. No Git remote is configured, so a hosted Actions run is explicitly
  unverified. Evidence: `tests/s04-evidence/ci-workflow-certification.json`.
- Audited AS-02 rekey beyond the existing unit method and found no operational path. Added
  `POST /api/v2/platform-accounts/{account_pub_id}/profiles/rekey`: it accepts no profile plaintext, requires
  the current profile version plus its exact active fenced lease, rotates to a fresh DEK/nonce/ciphertext with
  the next version's AAD, marks the prior version `SUPERSEDED`, and emits a reason-enum-only session event.
  Idempotency uses a tenant/account-scoped hashed key plus transaction advisory lock; exact replay returns the
  same profile and a conflicting replay fails. A stale lease bound to the superseded profile cannot write the
  new profile. The production Vault probe now also rotates a synthetic profile, verifies the old AAD is rejected,
  and deletes its probe key. Focused tests pass 28/28, full Python passes 145/145, generated OpenAPI has 87 paths
  with no drift, and the API is deployed. No authorized customer profile was fabricated. Evidence:
  `tests/s04-evidence/profile-vault-rekey-certification.json`.
- Separated outer Vault Transit KEK rotation from profile DEK rotation and tested both against production Vault.
  The admission CLI now exposes `--rotate-existing` only after rechecking a current database authorization; an
  unadmitted rotation probe is rejected. Runtime and deletion credentials receive 403 on rotate. On a synthetic
  account, the provision authority rotates `vault:v1` to v2, old ciphertext remains recoverable during the
  controlled transition, the fenced profile rekey emits a v2-wrapped DEK, and setting
  `min_decryption_version=2` blocks the retained v1 ciphertext without breaking the rewrapped profile. The probe
  key is then deleted. This proves mechanics and rollback behavior, not authorization to rotate any customer key.
- Audited lease release paths and found that `GeoCollectionWorkflow` released only on normal completion, its own
  cancel signal and Continue-As-New; Activity/persistence/downstream failure and external Temporal cancellation
  leaked the account-exclusive lease until TTL. Refactored release into a retrying idempotent `finally`, with real
  Temporal tests for a non-retryable adapter failure and external workflow cancellation. Profile seal and DEK
  rekey now mark the old-profile lease released in the same transaction as the new ACTIVE version; a replacement
  lease immediately acquires fencing token N+1. Focused Temporal/governance tests pass 19/19, full Python passes
  147/147, API and Worker are redeployed, and production contains zero active or expired-unreleased leases.
  Evidence: `tests/s04-evidence/session-lease-lifecycle-certification.json`.
- Audited `PlatformSessionLifecycleWorkflow` separately and found it was a registered status-string stub: it
  accepted caller account/scope values without authorization or a lease and returned `active` without holding
  session authority. It now requires tenant/account/scope/holder, performs the same current-authorization and
  ACTIVE-profile admission Activity, acquires a fenced lease, handles duplicate intervention and revocation
  Signals, returns a typed completed/revoked result, and releases from an idempotent retrying `finally` on
  success, timeout or external cancellation. Admission errors are now typed non-retryable Temporal errors rather
  than mismatched `ValueError` strings. The real Temporal suite passes 148/148; a deployed production Temporal
  probe passes 7/7 and confirms the PostgreSQL lease is released with the matching fencing token. Because
  production has no authorized account/profile, the probe used and removed an isolated synthetic fixture and is
  not counted as customer custody or live-platform admission. Evidence:
  `tests/s04-evidence/production-session-lifecycle.json`.
- Audited account revocation and found duplicated router implementations plus a commit-before-Temporal gap:
  Temporal connection/start failure left a revoked account and erased database DEK authority behind a `running`
  request with no durable workflow. Revocation staging is one shared transaction. It initially started the
  terminal workflow before commit and rolled back on Temporal outage; the later `s04_0013` outbox audit found that
  rollback was unsafe because owner revocation must fail closed even while Temporal is unavailable. Account,
  profile, device, task and intervention revocation now commit atomically with an `account_revocation` start
  command. The outbox Worker retries until the deterministic per-account workflow is durable and converges
  AlreadyStarted after an accepted-before-ack crash. The deletion Activity locks the request and propagates
  device, terminal task, intervention and capability revocation,
  and returns a fully typed Temporal result instead of the unserializable `dict[str, object]` found by the first
  production probe. Duplicate API revocation and duplicate Activity execution are idempotent. Full Python passes
  152/152; the deployed production outbox/Temporal/PostgreSQL/Vault probe passes 12/12, including single-attempt
  dispatch, persisted Temporal run ID, profile material purge,
  external key destruction and rejection of retained ciphertext after same-name key recreation. Its isolated
  synthetic fixture and recreated key were removed; this is not customer authorization or backup deletion
  approval. Evidence: `tests/s04-evidence/production-account-revocation.json`.
- Closed authorization-revocation bypasses across the challenge path. A revoked customer or operator account can
  no longer receive a new authorization; customer pairing requires a pairable account plus an authorization whose
  `valid_from <= now < valid_until`; operator intervention creation checks the same scope and forbidden-action
  snapshot. Both server-side intervention completion and customer-terminal bind/completion recheck current
  authorization, so expiry or revocation during a pending challenge cannot reactivate the account or issue/use a
  terminal task. Capability lease validation already performed this live check and remains fail-closed.
  Profile enroll, fenced seal, fenced DEK rekey and profile health/decryption probes now also require a current
  authorization; an unexpired session lease alone cannot authorize a write after owner authority expires.
  Focused integration passes 12/12, full Python passes 149/149, and the deployed browser/mock matrices remain
  33/33 and 28/28. Evidence:
  `tests/s04-evidence/authorization-revocation-propagation.json`.
- Made account revocation irreversible at the database boundary. The quarantine endpoint previously overwrote
  `revoked` with `quarantined`, and application-level read-then-write guards could still lose a race against a
  concurrent revoke. Migration `s04_0012` installs a PostgreSQL `BEFORE UPDATE OF state` trigger that raises
  stable constraint `ck_platform_account_revoked_terminal` (`23514`) for every revoked→non-revoked transition;
  quarantine also returns an explicit 409. A production transaction probe passes 6/6, including direct SQL
  reactivation rejection, retained revoked state, enabled-trigger metadata and complete fixture cleanup.
  Pre-migration PostgreSQL backup
  `.production-backups/20260725T031836Z/postgres-pre-s04-0012.dump` is 1,805,176 bytes, has 847 catalog entries
  and SHA-256 `77d12a6f1e1efee861b3064eb55fc38b0acbb035a70a786d74460a54d6f9e96f`.
  Production is at `s04_0012`; Python passes 149/149 and browser/mock acceptance remains 33/33 and 28/28.
  Evidence: `tests/s04-evidence/production-revoked-account-terminal-state.json`.
- Fixed authorization replacement semantics. Both customer and operator endpoints previously appended grants and
  selected the one with the farthest `valid_until`, allowing an older, longer, wider authorization to override a
  newer downgrade. They now lock the account, revoke every prior grant and insert the replacement in one
  transaction. Capability leases, session leases, pending/paired interventions and issued terminal tasks outside
  the new scopes or inside new forbidden actions are revoked/released in that same transaction. Updating an
  already-active account no longer incorrectly resets it to `owner_authorizing`. Regression tests prove a shorter
  narrow grant defeats the old long grant and that restoring authority does not revive the old terminal task.
  Evidence: `tests/s04-evidence/authorization-replacement-propagation.json`.
- Reopened the allegedly fixed FORCE-RLS report parent-row defect after the 150-test full suite reproduced it.
  The old no-op `ON CONFLICT DO UPDATE` plus post-upsert key-share pin was not a reliable logical CAS under
  concurrent report/evidence writers. Evidence parent creation now takes a transaction advisory lock bound to
  tenant+content-hash+kind, key-share pins an existing parent, or creates exactly one new parent. A 16-writer
  same-key test and report API were run together for five rounds (10/10), then the full suite passed 151/151.
  Post-deploy browser/mock acceptance remains 33/33 and 28/28. Evidence:
  `tests/s04-evidence/evidence-parent-concurrency.json`.
- Replaced direct collection workflow start with a transactional Temporal-start outbox. The API previously
  committed a `starting` run, then called Temporal; crashes before start returned a phantom run, crashes after
  acceptance lost the acknowledgement, and idempotent retries simply returned `starting`/`start_failed` without
  recovery. `s04_0013` atomically inserts `integration.workflow_start_command` with the run. The existing outbox
  Worker claims commands with `SKIP LOCKED`, delays/reclaims failed dispatches, converges accepted-before-ack
  crashes through the fixed workflow ID/AlreadyStarted path, and writes the Temporal run ID under explicit tenant
  context. `GeoCollectionWorkflow` now durably marks completed, failed or cancelled runs; failure/cancellation
  cleanup still releases the fenced session lease. An idempotency replay compares the persisted project/config/
  account/challenge contract and returns 409 for a changed body rather than silently returning the old run.
  Unit/integration recovery passes and full Python is 152/152.
  A production Temporal probe passes 8/8: the command was dispatched once and IDs persisted; the downstream
  collection correctly failed `adapter_not_configured` because no AS-08 live adapter is admitted, and the run was
  marked failed rather than left running. Synthetic rows were removed. Pre-migration backup
  `.production-backups/20260725T033905Z/postgres-pre-s04-0013.dump` is 1,806,297 bytes with 849 catalog entries
  and SHA-256 `52fde1280cda0d9cda568e974e850e0a80e9223dfa0c698a7fdc5bc6082fead9`.
  Evidence: `tests/s04-evidence/production-workflow-start-outbox.json`.
- Closed the remaining historical collection-run/Temporal terminal-state drift. A production inventory found one
  pre-outbox run still marked `running` although its Temporal execution was `FAILED`. Migration `s04_0014`
  adds terminal reconciliation metadata and backfills observation commands only for nonterminal
  `starting`/`start_failed`/`running` rows; it deliberately excludes `completed_with_failures` and `skipped`.
  The outbox now periodically describes started collection executions and converges Temporal
  `COMPLETED`/`FAILED`/`CANCELED`/`TERMINATED`/`TIMED_OUT` into durable database terminal state. After deployment,
  the historical row converged to `failed/temporal_failed`; a read-only production certification passes 5/5,
  Temporal independently reports `FAILED`, and no nonterminal run lacks a durable command. Full Python passes
  152/152 and post-deploy browser/mock checks pass 33/33 and 28/28. Pre-migration PostgreSQL backup
  `.production-backups/20260725T040250Z/postgres-pre-s04-0014.dump` is 1,810,559 bytes with 859 catalog entries
  and SHA-256 `9547540197e01a95b4d854f42a838a938776a66bfcfc609d5050ef1fdb99691e`.
  Evidence: `tests/s04-evidence/production-workflow-terminal-reconciliation.json`.
- Reopened the earlier API→workflow→activity observability claim after the workflow-start path moved behind the
  transactional outbox. The outbox initially stored no trace carrier and connected to Temporal without a tracing
  interceptor, so the old production observability snapshot no longer proved the deployed path. Migration
  `s04_0015` persists only W3C `traceparent`/`tracestate` and excludes baggage; the outbox restores the parent
  context and starts Temporal with `TracingInterceptor`. A fresh production probe passes 10/10: the same trace
  contains `POST /api/v2/collection/runs`, workflow start/run/complete and activity start/run spans across
  `geo-platform-v2-api`, `geo-platform-v2-outbox-worker` and `geo-platform-v2-worker`, and the workflow-start
  span's parent is the API span. The isolated project/run fixture was removed. Python remains 152/152 and
  post-deploy browser/mock checks pass 33/33 and 28/28. Pre-migration backup
  `.production-backups/20260725T041418Z/postgres-pre-s04-0015.dump` is 1,811,469 bytes with 860 catalog entries
  and SHA-256 `cfca381529cdd97c099eba295706c1dd6322db5aa8d43d51e8c31c37cde05d46`.
  Evidence: `tests/s04-evidence/production-outbox-trace.json`.
- Corrected the remaining accepted-before-ack recovery defect. The outbox caught
  `WorkflowAlreadyStartedError` but originally discarded its run ID; after an initial source fix, a real
  production Temporal probe proved version 1.27 may return this exception with no run ID at all. The recovery
  path now describes the fixed workflow ID to obtain the server-authoritative run ID and fails closed when it
  cannot. A production workflow was deliberately accepted and held open before its command was inserted; the
  outbox converged in one attempt and persisted the identical run ID in the command and collection run. The
  isolated 6/6 fixture was removed, full Python passes 152/152, and browser/mock checks remain 33/33 and 28/28.
  Evidence: `tests/s04-evidence/production-workflow-already-started.json`.
- Closed the missing Temporal history recovery gap. Previously, `describe()` returning authoritative
  `NOT_FOUND` raised into the Worker loop forever, leaving a known-started database run reported as `running`
  with no runtime authority. Reconciliation now treats only RPC `NOT_FOUND` as a fail-closed terminal condition,
  records command status `NOT_FOUND`, and changes a nonterminal collection run to
  `failed/temporal_history_missing`; transient RPC errors still retry and the command is never redispatched.
  A production isolated probe passes 4/4 and its rows are removed. Full Python remains 152/152.
  Evidence: `tests/s04-evidence/production-workflow-missing-history.json`.
- Added terminal reconciliation for account-revocation commands. Previously a completed Activity updated the
  request, but the durable command remained terminal-unknown; Workflow failure, cancellation, termination or
  timeout could leave the request `running` forever. Migration `s04_0016` adds a bounded request error code and
  expands the reconciliation index. Completion records command `COMPLETED`; abnormal Temporal terminal states
  produce explicit failed/cancelled request state while preserving the irreversible account `revoked` boundary.
  The refreshed production Temporal/PostgreSQL/Vault probe passes 12/12, including terminal command convergence,
  profile purge and key non-reactivation. Python remains 152/152 and browser/mock checks pass 33/33 and 28/28.
  Pre-migration backup `.production-backups/20260725T043914Z/postgres-pre-s04-0016.dump` is 1,811,541 bytes with
  860 catalog entries and SHA-256
  `b8dd63a54a7e56bb4d4906e3b6a1387333af23f909a9ac208755a2f8991bacd5`.
- Replaced all three direct API→Temporal Signal boundaries with an ordered transactional outbox. Run
  pause/resume/cancel formerly signalled before committing database state; server and customer-terminal
  intervention completion committed the authoritative result first, then swallowed any Signal RPC failure.
  Migration `s04_0017` adds a restricted Signal command table. State and command now commit atomically; the
  outbox restores W3C trace context, retries stale claims, treats only authoritative workflow-not-found as
  terminal and prevents later commands for the same workflow from overtaking an earlier pending command.
  Integration tests cover outage/reclaim/delivery. A follow-up HA audit found that a second outbox Worker could
  otherwise claim a Signal while the matching start command was still dispatching and prematurely mark it
  workflow-not-found. Signal claiming now has a database start barrier. A controlled production probe stops the
  background outbox, proves an early claim is blocked, dispatches the start, then delivers pause→resume→cancel in
  recorded order and once; both Temporal and PostgreSQL report cancelled (11/11), and fixture rows are removed.
  Python passes 152/152 and browser/mock checks remain 33/33 and 28/28. Pre-migration backup
  `.production-backups/20260725T044819Z/postgres-pre-s04-0017.dump` is 1,811,602 bytes with 860 catalog entries
  and SHA-256 `425803d934fc8c71e94c3d4151092e6e2c098a9a147d89c05d41990ecf23b9b3`.
  Evidence: `tests/s04-evidence/production-workflow-signal-outbox.json`.
- Closed collection-run control terminal-state regression. The control endpoint accepted cancel for
  completed/failed/cancelled/skipped runs and rewrote them to `cancelling`; because terminal reconciliation had
  already completed, that could become permanent. It now rejects every terminal control with stable
  `run_terminal`, rejects pause/resume while cancelling, and treats duplicate pause/resume or repeated cancel as
  idempotent no-ops without another Signal. A read-only production probe against a real failed run passes 6/6:
  HTTP 409, stable error, state unchanged and no Signal command added. Python remains 152/152 and post-restart
  browser/mock acceptance passes 33/33 and 28/28.
  Evidence: `tests/s04-evidence/production-run-terminal-guard.json`.
- Added durable request idempotency to run-control Signals. Temporal Python has no Signal request-ID parameter,
  so retry safety cannot depend on the transport. Migration `s04_0018` stores a SHA-256
  `Idempotency-Key` receipt and canonical workflow/signal/args contract hash, never the raw key. The endpoint now
  requires the header before mutation, replays the same contract without another command and returns stable
  `idempotency_conflict` if the key is reused for another action. Operations Web and generated OpenAPI client are
  updated. The production Signal probe passes 12/12 including unique hashed receipts; TypeScript and OpenAPI
  guards pass, Python is 152/152, full four-application E2E is 216/216, and production browser/mock checks remain
  33/33 and 28/28. Pre-migration backup
  `.production-backups/20260725T050335Z/postgres-pre-s04-0018.dump` is 1,815,490 bytes with 869 catalog entries
  and SHA-256 `b2dbb281c809cac7d43d7e7e89415c0ebf99735becebb5fbd2d27c2b5df18058`.
- Closed the concurrent receipt-insert conflict path. The initial implementation returned stable 409 when a
  receipt already existed at precheck, but two different workflows racing with one key could both pass that
  check; the losing INSERT raised after state mutation and escaped as 500. The endpoint now catches that database
  race, rolls back the losing state and returns `idempotency_conflict`. A threaded API integration proves one
  200, one 409 and one receipt. A production two-session database probe passes 6/6, including hash-only storage
  and rollback of the loser. Post-restart browser/mock acceptance remains 33/33 and 28/28.
  Evidence: `tests/s04-evidence/production-signal-idempotency.json`.
- Hardened collection run terminal monotonicity against concurrent and direct-database bypass. The API-only
  terminal check still allowed a control request and terminal Activity to race on a stale row, and privileged
  SQL could reactivate a terminal run. Control now locks the row with `FOR UPDATE`. Migration `s04_0019`
  installs `ck_collection_run_terminal_state`, rejecting any completed/completed-with-failures/failed/cancelled/
  skipped transition to a different state with SQLSTATE 23514. The production probe passes 8/8: stable HTTP 409,
  no Signal, direct SQL reactivation rejected by the named constraint and terminal state retained. Python passes
  152/152; browser/mock acceptance is 33/33 and 28/28. Pre-migration backup
  `.production-backups/20260725T051810Z/postgres-pre-s04-0019.dump` is 1,816,048 bytes with 870 catalog entries
  and SHA-256 `ed92bc73ea83f95d81cc2e639ea3ce7ce5ed7ba87e34d158c2d04835f56e1283`.
- Deployed the production Vault Transit topology that was previously only a source template. Vault 2.0.3 now
  runs loopback-only with TLS 1.3, persistent integrated Raft storage and mlock; Shamir initialization uses a
  2-of-3 threshold and the initial root token was revoked. Runtime, account-provisioning and key-deletion tokens
  are root-owned and policy-separated; systemd exposes only runtime encryption/decryption to the API and only
  deletion to the revocation Worker. The admission CLI refuses accounts without a current database authorization.
  A non-customer production probe proves encrypt/decrypt, all cross-role denials, key deletion and retained
  ciphertext non-reactivation after same-name recreation, then removes its probe key. A stop/start, manual
  threshold unseal and repeated ACL/crypto probe pass. Root-only backups were captured at
  `.production-backups/20260725T013859Z`: Raft is 33,607,168 bytes
  (`c649957f…120389a`), and authority/TLS/tokens are 20,480 bytes
  (`84826565…4219b1`). API and all Workers remain active, `/api/v2/health` is 200 and legacy `/geo` remains 401.
  This same-host deployment proves configured mechanics, not independent organizational custody or deletion of
  an authorized migrated customer profile. Evidence:
  `tests/s04-evidence/production-vault-transit.json`.
- Reproduced and fixed an intermittent real-PostgreSQL report integrity failure that an earlier green run had
  hidden. Both initial report rendering and revision rendering could insert a `report_artifact` after
  `EvidenceService.capture()` returned an upserted parent ID, yet FORCE RLS cross-schema FK lookup intermittently
  reported the parent absent. Capture now materializes and pins the exact tenant-scoped parent with
  `FOR KEY SHARE` through caller commit and raises a stable transaction invariant error if it cannot. The
  formerly failing vertical slice passes 21/21 consecutive stress runs, the complete Python suite passes
  144/144 twice after the fix, and the full package quality gate passes. The deployed API then passes 33/33
  production browser checks and 28/28 runtime mock checks.
- Closed an Activity ack-loss/concurrency accounting gap in collection result persistence. Business-key
  uniqueness already made a retry of the same task idempotent, but two distinct task completions could read the
  same run counter and lose one increment. The Activity now locks the collection run before deduplication and
  counter mutation. A two-thread real-PostgreSQL regression proves two distinct tasks produce two rows,
  `completed_tasks=2` and terminal `completed`; the complete Python suite passes 153/153. The production Worker
  was restarted with the exact source hash and remains active. Pre-deploy backup
  `.production-backups/20260725T052505Z/postgres-pre-activity-accounting.dump` is 1,867,320 bytes with 1,034
  catalog entries and SHA-256 `894b7940ae86f309ebbc367ad6d31371d8db7c339b018c8ef64204b5f25254bd`.
  Evidence: `tests/s04-evidence/production-activity-result-accounting.json`.
- Closed the report human-review Activity's commit-before-ack gap. Migration `s04_0020` adds a tenant-scoped,
  partial unique workflow-operation receipt to `reporting.report_review`; exact retries return the original
  review ID, while a changed version/reviewer/decision/rationale fails and rolls back. This covers both repeated
  `changes_requested` delivery and recovery after review commit but before publish. The production synthetic
  probe passes 5/5 and removes all fixture rows; full Python passes 153/153 and post-deploy browser/mock checks
  pass 33/33 and 28/28. Pre-migration backup
  `.production-backups/20260725T053159Z/postgres-pre-s04-0020.dump` is 1,867,320 bytes with 1,034 catalog entries
  and SHA-256 `0020e255a24e4c80872816d6c570c3e5687678c055731febb8630a354e615294`.
  Evidence: `tests/s04-evidence/production-report-review-idempotency.json`.
- Closed answer-analysis replay and concurrency drift. The prior implementation deduplicated stored analysis
  rows but returned newly generated, non-persisted analysis/outbox IDs on retry; concurrent first delivery could
  also race while creating the analysis run, and the same answer public ID accepted changed raw-answer fields.
  Analysis-run and answer-analysis creation now use atomic conflict-returning writes, every retry returns the
  authoritative stored analysis/run/outbox IDs, and raw-answer contract drift fails the transaction. A two-thread
  real-PostgreSQL regression and the cleaned production probe each prove one analysis, one run and one outbox
  event plus drift rejection. Full Python passes 154/154; the production S02 Worker is active. Evidence:
  `tests/s04-evidence/production-analysis-idempotency.json`.
- Corrected the evidence storage identity boundary. Immutable bytes remain shared through the content-addressed
  MinIO key, but each capture occurrence now has its own stable evidence public ID and isolated project,
  access-class, source and authorization provenance. The former `(tenant, sha256, kind)` uniqueness could
  silently reuse the first occurrence's metadata for later captures of identical bytes. Migration `s04_0021`
  replaces it with a non-unique lookup index; capture retries serialize on evidence ID and reject any metadata
  drift. Real-PostgreSQL concurrency/replay tests pass, full Python is 155/155, and the cleaned production
  PostgreSQL+MinIO probe passes 5/5. Browser/mock acceptance remains 33/33 and 28/28. Pre-migration backup
  `.production-backups/20260725T054215Z/postgres-pre-s04-0021.dump` is 1,867,786 bytes with 1,035 catalog entries
  and SHA-256 `39ae9416118808b713e0932f910867f46cac895df87be9ec8dce98dfeccbc3ac`.
  Evidence: `tests/s04-evidence/production-evidence-occurrence-identity.json`.
- Replaced the collection workflow's downstream-event placeholder with a durable, idempotent integration path.
  Migration `s04_0022` adds one `collection.run.completed` outbox event per tenant/run and backfills all four
  completed production runs (4/4). New Temporal histories atomically enqueue one uniquely identified
  `AnswerAnalysisWorkflow` command per completed answer when the project has an admitted brand; missing brand or
  answers is recorded as a failed admission rather than claimed as analysis. Workflow patch markers retain old
  history replay compatibility. The outbox dispatcher now supports `answer_analysis`, and the ClickHouse
  analytics consumer filters its owned event types so collection events cannot poison the queue; this closes a
  production loop that had been logging one unsupported-event failure per second. Temporal/PostgreSQL tests and
  the cleaned production collection→command→Temporal→analysis→analytics-outbox probe pass 5/5; production domain
  routing passes 4/4, full Python passes 157/157, and all Workers are active. Pre-migration backup
  `.production-backups/20260725T054835Z/postgres-pre-s04-0022.dump` is 1,867,681 bytes with 1,035 catalog entries
  and SHA-256 `eb372da43bf2370e82e4793c1aaeb5aa84a10758224f9dac55b2f5459a181c63`.
  Evidence: `tests/s04-evidence/production-collection-completion-outbox.json`,
  `tests/s04-evidence/production-outbox-domain-routing.json` and
  `tests/s04-evidence/production-collection-analysis-fanout.json`.
- Closed collection-to-analysis lineage loss. The fan-out previously used `query` where the analytics contract
  consumes `query_text`, raw answers had no collection run/config columns, and the ClickHouse projection read
  root-level run lineage that the outbox never emitted. Migration `s04_0023` adds indexed nullable
  `run_pub_id`/`config_version_pub_id` truth to raw answers; task persistence stores the canonical
  query/model/region/mode/adapter matrix and rejects same-business-key result drift; PostgreSQL answer,
  analytics outbox and ClickHouse answer/run facts now retain the same collection run. Answer API/OpenAPI expose
  the lineage fields and generated clients have no drift. The cleaned production PostgreSQL→Temporal→S02
  Worker→outbox→ClickHouse probe passes 8/8, TypeScript packages and mypy pass, Python is 157/157, and
  browser/mock acceptance remains 33/33 and 28/28. Pre-migration backup
  `.production-backups/20260725T060336Z/postgres-pre-s04-0023.dump` is 1,868,586 bytes with 1,036 catalog entries
  and SHA-256 `3af0acda17d231b5035e8fb175dbceab2a3bf4f14ff19e7a263fc01e61b7ee5e`.
  Evidence: `tests/s04-evidence/production-collection-analysis-fanout.json`.
- Hardened collection result admission and fan-out acknowledgment. Answer text and screenshot references now pass
  fail-closed DLP before any task row or workflow command can be written; a rejected result is a non-retryable,
  label-only Activity failure. Completion fan-out records expected versus created command counts, leaves missing
  brand/answers and partial fan-out unacknowledged, and marks the event published only when every completed
  answer has a durable analysis command. A real-PostgreSQL regression proves partial→retry→complete recovery and
  zero secret-bearing task insertion. The refreshed production PostgreSQL→Temporal→S02→ClickHouse probe passes
  9/9 including pre-storage DLP; mypy and Python 157/157 pass and all Workers remain active.
- Removed the false report contract that treated publication as customer delivery/confirmation. `s04_0024`
  enforces one delivery row per tenant/report/recipient; only a published report can be delivered, and an
  authenticated customer can list and confirm only the row addressed to that exact subject. Exact retries return
  the authoritative delivery ID and retain the first confirmation timestamp; changed confirmation content fails
  with a stable conflict. `s04_0025` adds an idempotent audit boundary so delivery and recipient confirmation
  each produce exactly one actor-attributed report event. Report Studio now publishes first and creates a
  targeted delivery separately; Customer Web uses the real list/confirm API instead of the prior disabled
  placeholder. The cleaned production PostgreSQL probe passes 11/11, while explicitly qualifying itself as
  synthetic storage/mechanism evidence rather than a real-customer acceptance:
  `tests/s04-evidence/production-report-delivery-confirmation.json`.
- Production advanced through `s04_0024` and `s04_0025` after two restricted PG16 backups:
  `.production-backups/20260725T062238Z/postgres-pre-s04-0024.dump` is 1,869,114 bytes, mode `0600`, has 1,037
  catalog entries and SHA-256 `689616ad9f4d46ba0b1ae90e106b33d858daeb93ca7362cab62b26fb6b4d555d`;
  `.production-backups/20260725T062740Z/postgres-pre-s04-0025.dump` is 1,869,477 bytes, mode `0600`, has 1,038
  entries and SHA-256 `650f607ee6ec6d8e1cb9505e87e9ab6746d251e3b0bed17a9459ced4fda18c3a`.
  The regenerated OpenAPI/client has 89 paths and no drift.
- During a production preflight diagnostic, psycopg rejected a SQLAlchemy DSN and its exception text expanded the
  current owner credential. The exposed value is not reproduced in any evidence. All three configured PostgreSQL
  role credentials were immediately rotated to distinct replacements, restricted runtime/compose configuration
  was updated, and the API plus three Workers were restarted. The post-rotation 6/6 probe authenticates every
  DSN as its intended role, proves runtime roles are non-superuser/non-BYPASSRLS, checks both configuration files
  are not world-readable, verifies all consumers active and API health green, and records neither credential
  values nor digests: `tests/s04-evidence/production-postgres-credential-rotation.json`.
- The final rerun for this milestone passes Python 157/157, 66 TypeScript tests, lint, repository formatting,
  strict TypeScript, mypy, OpenAPI drift/frontend guards, 12 production builds and bundle/legacy-route guards.
  The complete four-application desktop/tablet/mobile E2E passes 216/216 in 226,083 ms with zero unexpected,
  skipped or flaky tests. Rebuilt production assets pass 33/33 authenticated browser checks and 28/28 runtime
  mock scans with zero console, page, request or HTTP errors. RLS was recertified at `s04_0025`: all 80 tenant
  tables enable and force RLS, both runtime roles are unprivileged and `row_security=off` is blocked.
- Refreshed production counts at `s04_0025`: 1 tenant, 5 users/memberships/projects/configs, 77 queries, 8
  historical runs, 192 tasks, 146 answers/analyses, 2,754 citations, 533 evidence assets, 0 reports/deliveries,
  1 migration run and 13 migration watermarks. ClickHouse retains 146 answer facts, 2,754 citation facts, 146
  run events and 1,022 daily metrics. Integration outbox has 150 rows; the only four unpublished rows are the
  deliberately unacknowledged `collection.run.completed` events for historical runs without admitted
  brand/answer fan-out. No synthetic report-delivery row/event remains:
  `tests/s04-evidence/production-runtime-data-counts.json`.
- Re-audited the authenticated identity boundary instead of treating the external legacy/OIDC subject as a
  business actor ID. `Principal.actor_pub_id` now fails closed unless the current membership projects a platform
  user public ID; actor/recipient fields across projects, collection, evidence, reports, exports and Intelligence
  use that ID. Migration `s04_0026` transactionally normalizes every known legacy subject and adds resolver,
  rationale and timestamp audit fields to Intelligence appeals. The production certificate checks 18 actor
  columns and finds zero legacy-subject residue without emitting any identifier or secret:
  `tests/s04-evidence/production-actor-identity.json`.
- Closed the report and Intelligence role-boundary drift in API and all three viewport contracts. Analysts author
  reports/actions and Intelligence analyses/appeals; reviewers approve/publish/deliver reports and record human
  verdicts/resolutions. An appeal resolver must differ from both the submitter and the latest verdict reviewer.
  Customers can list, fetch, comment on and confirm only a published report explicitly delivered to their active
  same-tenant customer membership; internal reviews/events/actions and other-customer comments are not projected.
  The real PostgreSQL integration suite covers cross-role denials, customer non-enumeration and actor audit IDs.
- Production advanced from `s04_0025` to `s04_0026` after restricted PG16 backup
  `.production-backups/20260725T072656Z/postgres-pre-s04-0026.dump` (1,869,984 bytes, mode `0600`, 1,039 catalog
  entries, SHA-256 `3642e30a2f41c5ebe382aeb516ffc93aa1d6618a147195812b31e8f704e39564`).
  All four V2 services are active; all 12 containers run and the six stateful dependencies with health checks are
  healthy. RLS remains ENABLE+FORCE on 80/80 tenant tables, runtime roles remain unprivileged, and the production
  inventory is unchanged except for the schema revision.
- The refreshed complete gate passes Python 160/160, 66 TypeScript tests, lint, formatting, strict TypeScript,
  mypy on 94 files, OpenAPI regeneration/drift at 89 paths, all production build/bundle/legacy-route guards and
  four-app desktop/tablet/mobile E2E 216/216 in 229,443 ms with zero unexpected, skipped or flaky cases.
  Production acceptance passes 33/33 and the live mock scan 28/28 with zero console, page, request or HTTP errors.
  `/portal`, `/ops`, `/client` and `/api/health` remain direct 200 responses; `/geo` retains its existing 401
  login boundary. The legacy system was not switched, redirected, overwritten or removed.
- Closed the remaining source-owned Report Studio authoring gap instead of treating a read-only/local editor as
  live integration. `POST /api/v2/reports/{report_pub_id}/versions` now requires `report:write` and a bounded
  `Idempotency-Key`, persists only its SHA-256 plus a canonical revision contract hash, copies frozen facts on the
  server, creates an immutable version, binds same-tenant evidence references and renders HTML/DOCX/PDF/XLSX
  artifacts. Exact retries converge, payload drift returns a stable conflict, concurrent requests serialize, and
  a retry resumes missing artifacts. Analysts can save through the generated client; reviewers and published
  reports are read-only. Real PostgreSQL/MinIO integration covers replay, concurrency, rollback, role isolation
  and partial-artifact recovery. Production certification records zero report rows and therefore does not claim
  a populated customer report acceptance:
  `tests/s04-evidence/production-report-authoring.json`.
- Production advanced from `s04_0026` to `s04_0027` only after a validated PG16 backup at
  `.production-backups/20260725T075810Z/postgres-pre-s04-0027.dump` (1,870,141 bytes, mode `0600`, 1,039 catalog
  entries, SHA-256 `082d8620aa1bee580d4e5beb800a8e00266a2b51e5232deb6d55d534564b8093`).
  The actor certificate still reports zero legacy-subject residue across 18 columns, and the report-authoring
  certificate verifies the new columns, unique partial index, hash-pair constraint, generated OpenAPI/client
  boundary and all four active V2 services. RLS remains ENABLE+FORCE on 80/80 tenant tables.
- The final `s04_0027` quality rerun passes Python 160/160, 66 TypeScript tests, lint, formatting, strict
  TypeScript, mypy on 94 files, 12 production builds and all release/legacy-route guards. OpenAPI now has 90
  paths and regenerates without drift; hashes are
  `2e8f3ca9b540670a91f8a45e3e9cb41db7c426947949dcbb5932166d0514458f` and
  `2c4ddcc44ac2401f77d4d0b08fdfcf27bb0dcc05f9b5faedf340c5c80da11f94`.
  The four-app desktop/tablet/mobile E2E passes 216/216 in 226,147 ms with zero unexpected, skipped or flaky
  cases: `tests/s04-evidence/e2e-results-s04-0027-final.json`.
- Rebuilt production assets pass 33/33 authenticated browser checks and 28/28 live mock/fixture scans with zero
  console, page, failed-request or HTTP-response errors. Production inventory at `s04_0027` remains 1 tenant, 5
  users/memberships/projects/configs, 77 queries, 8 runs, 192 tasks, 146 answers/analyses, 2,754 citations, 533
  evidence assets, 0 reports/deliveries, 1 migration run and 13 watermarks; ClickHouse retains 146 answer facts,
  2,754 citation facts, 146 run events and 1,022 daily metrics. `/portal`, `/ops`, `/client` and `/api/health`
  remain direct 200 responses and `/geo` remains the existing unauthenticated 401. Nginx production/source
  configuration hashes match; no legacy route was switched.
- Reopened the prior observability claim after verifying that production had no loaded business alert rules,
  Alertmanager or notification pipeline. `s04_0028` adds five bounded partial indexes and a
  `SECURITY DEFINER`/`search_path=pg_catalog` aggregate returning exactly 12 metric rows. Only the Worker role
  can execute it; API and PUBLIC cannot. A loopback exporter exposes fixed low-cardinality gauges without
  tenant/account/project/workflow/recipient labels. Twelve Prometheus rules, pinned Alertmanager, an allowlist-only
  64 KiB receiver and four Grafana panels are guarded in CI.
- Production advanced from `s04_0027` to `s04_0028` after validated PG16 backup
  `.production-backups/20260725T083238Z/postgres-pre-s04-0028.dump` (1,870,962 bytes, mode `0600`, 1,040 catalog
  entries, SHA-256 `a475c7e80cf8ed450dd782997c4a5f46f73f3e7f877eb9f36441d46a49435726`).
  Alertmanager is the 13th isolated production container; the business exporter and safe receiver raise the
  active V2 systemd count to six. A real `GeoCollectionAnalysisAdmissionBlocked` alert traversed
  Prometheus→Alertmanager→receiver→Loki without identifier or secret labels.
- The real alert exposed a second source-owned migration defect rather than being accepted as a permanent
  warning. All 146 migrated answers had V2 rebuilt analyses, but none retained its source collection run/config
  lineage, so the four backfilled completion events could not prove downstream convergence. The core migrator
  now maps `schedule_tick_id` into deterministic run/config IDs, fails closed on target lineage drift and
  acknowledges only legacy-history events for which task-answer, lineage and V2-analysis counts match exactly.
  A real PostgreSQL regression proves incomplete coverage remains pending, exact coverage converges once and a
  third call is a no-op.
- Before the production lineage repair, backup
  `.production-backups/20260725T084822Z/postgres-pre-lineage-reconciliation.dump` was validated at 1,876,519
  bytes, mode `0600`, 1,047 catalog entries and SHA-256
  `3830c1d58546da46787f6132719b31985d8bb86d99c9f937f2421b5c5e56320b`.
  The first immutable-snapshot rerun wrote 146 lineage links and reconciled 4/4 completion events; every ordinary
  entity mapping remained zero-write. The immediate second rerun wrote zero everywhere. Current production has
  146/146 linked and analyzed migrated answers, 14 watermarks, zero unpublished outbox rows, zero admission
  backlog and zero firing business alerts:
  `tests/s04-evidence/production-core-migration-lineage-rerun.json`,
  `production-core-migration-lineage-idempotent-rerun.json`,
  `production-runtime-data-counts.json` and `production-business-alerting.json`.
- The refreshed quality gate passes Python 167/167, 66 TypeScript tests, Ruff/formatting, strict TypeScript,
  mypy on 96 files, OpenAPI regeneration/drift at 90 paths, 12 production builds and every
  bundle/legacy-route/observability/CI guard. The unchanged four-app desktop/tablet/mobile E2E remains 216/216
  in 230,589 ms with zero unexpected, skipped or flaky cases:
  `tests/s04-evidence/e2e-results-s04-0028-final.json`. After the rebuild and migration repair, production
  browser and mock acceptance pass 33/33 and 28/28 with zero console, page, failed-request, HTTP-response or
  fixture-marker errors. `/portal`, `/ops`, `/client`, `/api/health` and `/api/v2/health` retain their expected
  states; `/geo` remains the unchanged legacy 401 boundary.
- Reopened the Anti-GEO “calibration complete” claim because the previous evaluator accepted only an in-memory
  synthetic fixture and had no governed production dataset/admission truth. Migration `s04_0029` adds five
  tenant-scoped ENABLE+FORCE-RLS tables for versioned external datasets/cases, evaluation runs/results and model
  admissions. Dataset approval and model admission require independent actors; the source artifact is
  evidence-bound, DLP-clean and retained while approved; training/holdout propagation-cluster overlap is rejected.
  Generated list/write contracts and the Intelligence calibration workspace cover registration, approval,
  evaluation and admission. Production has zero approved external label sets and zero qualified admission chains,
  so the external calibration gate remains explicitly open:
  `tests/s04-evidence/anti-geo-evaluation-boundary.json`.
- The first post-`s04_0029` production browser run failed 12/45 checks and exposed a real Operations integration
  defect: the live application deliberately discarded its data contract and rendered fixture/empty state.
  `/api/v2/operations/lifecycle` now returns one bounded, tenant-scoped, role-authorized and secret-free projection
  of project runs, account authorization/health/leases, interventions, safe session events and activity.
  Operations Web consumes it through the generated client with validated identity headers; fixtures remain
  test-only. Real PostgreSQL tenant/role coverage passes, focused three-viewport E2E passes 30/30, and the repeated
  production overview/sessions/interventions/events matrix passes 12/12 with zero runtime errors.
- Production was backed up before migration at
  `.production-backups/20260727T035755Z/postgres-pre-s04-0029.dump` (1,877,800 bytes, mode `0600`, 1,032 PG16
  catalog entries, SHA-256 `06f5f5926e28f5142ef2445800dc4286177883b0e5f628a20c5579db55fd6210`);
  the contemporaneous static archive is
  `.production-backups/20260727T035755Z/static-builds-pre-s04-0029.tar` (7,403,520 bytes, mode `0600`, SHA-256
  `0b7ca4c6cf8f95131d04d43f3145243655b244f66005a43fa0b1a29b159020be`).
  PostgreSQL advanced idempotently to `s04_0029`; all 85 tenant tables enable and force RLS, both runtime roles
  remain unprivileged, all 13 containers and six V2 units are running, and `geosys.service` remains active.
- The final source gate passes Python 173/173, 168 TypeScript/Node tests, Ruff/formatting, strict TypeScript,
  mypy on 98 source files, 12 production builds and all OpenAPI/bundle/DLP/legacy-route/observability/CI guards.
  OpenAPI has 97 paths; its source and generated TypeScript hashes are
  `2da140787236a2395018b0062d75402531990b49c4a5a697a04235f726cecf26` and
  `63d1bd0e89c2d9bed7aa471412a1259df3852abd16306a6995b066002f1656b9`.
  The final four-app desktop/tablet/mobile E2E passes 411/411 in 489,091.42 ms with zero unexpected, skipped or
  flaky cases: `tests/s04-evidence/e2e-results-s04-0029-final.json`.
- Rebuilt production assets pass 45/45 authenticated browser checks and 29/29 runtime mock scans. All browser
  console, page, failed-request and HTTP-error counts are zero; evidence DLP passes over 32 output roots, 49
  bounded files and 390 JSON reports. Production remains at 1 tenant, 5 users/memberships/projects/configs, 77
  queries, 8 runs, 192 tasks, 146 answers/analyses, 2,754 citations, 533 evidence assets, 0 reports/deliveries,
  0 Anti-GEO datasets/runs/admissions, 1 migration run and 14 watermarks. All four historical completion events
  and 146 migrated answers remain reconciled, and unpublished outbox count is zero. The consolidated read-only
  release certificate verifies all 13 production containers, six V2 units plus `geosys`, version, routes, schema,
  data counts, backups, OpenAPI and the preserved 7/10 unified-gate result without recording secrets:
  `tests/s04-evidence/production-release-s04-0029.json`.

## Not completed

- Populated production acceptance for answer citation/evidence/anchor/history relations remains open because the
  mapped customer workspace exposes zero answer cards. Source, PostgreSQL and three-viewport browser contract
  evidence pass, but no production answer was fabricated.
- Populated production acceptance for the report detail/write/delivery family remains open because both legacy
  and persistent V2 production report tables are empty. The cleaned synthetic delivery probe certifies storage,
  idempotency, recipient isolation and audit attribution only; it does not manufacture a human approval,
  real-customer delivery or populated report-shadow result.
- Populated production acceptance for Intelligence history remains open because production has no investigation
  or content-version rows. Source/integration and production route/empty-state evidence pass, but no synthetic
  case was inserted to manufacture a production visual diff.
- Per-account encrypted profile migration/cutover and plaintext retirement proof. Profile owner/platform/scope
  resolution remains unresolved, so no plaintext profile was guessed into the Vault.
- Shadow execution of report artifacts after a V2 report exists; the migrated target reconciliation covers all
  populated legacy task/answer/eligibility/citation/KPI/admitted-evidence slices (legacy report rows are empty).
- Explicit production deletion-propagation certification across primary data, backups and evidence indexes.
- Independent organizational KMS/HSM custody and deletion of an authorized migrated production profile. The
  same-host production Vault mechanics and restricted backups now pass, but do not prove that external authority.
- Remaining AS profile custody gates are open. The owner/platform/scope mapping needed for safe per-account
  encryption and authority cutover has not been established.
- A restricted customer-terminal extension and signed task protocol are deployed, but no authorized customer
  terminal installation or native OTP/QR/Push/passkey/face/graphical platform canary is available. Artifact
  readiness and the passing local Chromium runtime fixture are not live challenge verification.
- The transitional production identity bridge no longer trusts browser actor headers: it validates the legacy
  HttpOnly cookie and current V2 mapping/membership. The OIDC verifier, browser PKCE flow and privacy-preserving
  binding lifecycle are deployed but not enabled; final production IdP registration/credentials,
  passkey/human verification and four missing real roles remain open.

## Safety and qualification

- No live external platform credential has been found or assumed. Adapter readiness is not treated as login/read/
  draft/publish verification.
- The final production metadata audit confirms zero admitted platform accounts, profiles, active profile DEKs,
  adapters and current account authorizations. Consequently the 13,063 legacy sensitive candidates cannot be
  assigned, imported, cut over or destroyed without new owner/legal/customer-terminal evidence:
  `tests/s04-evidence/external-gates-audit.json`.
- Legacy plaintext session inventory will report only metadata and digests; no secret values may be printed.
- The persistent goal remains active. The unified section 18 completion definition is not satisfied while the
  external identity, profile custody, customer-terminal and live capability gates above remain open.
