# S00 — Platform contracts and executable foundation

Updated: 2026-07-24 15:05 (Asia/Shanghai)  
Status: complete

## Completed

- Fully read `IMPROVEMENT_PLAN.md` (1182 lines), `IMPLEMENTATION_STATUS.md` (208 lines) and `REPORT.md` (334 lines).
- Audited the legacy Flask application factory, SQLite `schema.sql` plus idempotent ALTER initialization, pytest layout, Gunicorn/systemd deployment and protected route implementation files.
- Created the pnpm 11 workspace and Turborepo with four React 19 / React Router 8 Framework Mode SPA applications, strict TypeScript, Vite and seven shared packages.
- Created the FastAPI shell, config loading, JSON structured logging, health/readiness, contract mock and Python domain/workflow/migration/test/tool/deploy boundaries.
- Created PostgreSQL, ClickHouse, Temporal, Temporal UI, MinIO and Redis Compose services. Every container is healthy.
- Connected the Temporal worker and executed the S00 workflow against the development server.
- Frozen architecture/storage/Temporal/routing ADRs and identity, time, error, pagination, idempotency, tenant/RBAC, audit, outbox and workflow-ID conventions.
- Generated the OpenAPI TypeScript client and compiled it through all four applications.
- Frozen S01–S03 file ownership, Alembic revision allocation, session templates and contract-gap/ADR process.
- Added Python, TypeScript, OpenAPI drift, Compose smoke and legacy-route protection CI jobs.
- Verified that all changes are under `platform-v2/`; legacy route hashes still match the S00 baseline.

## In progress

- None.

## Not completed

- Business pages and domain behavior are intentionally outside S00 and remain owned by S01–S03.

## Contract gaps

- None open.

## Modified files

- Root/toolchain: `package.json`, `pnpm-workspace.yaml`, `pnpm-lock.yaml`, `turbo.json`, `tsconfig.base.json`, `pyproject.toml`, `alembic.ini`, `Makefile`.
- Applications: `apps/{customer-web,operations-web,report-studio,intelligence-web}`.
- Shared packages: `packages/{design-system,api-client,auth,charts,evidence-viewer,workflow-ui,domain-types}`.
- Python: `api/`, `domain/`, `workflows/`, `migrations/`, `tests/`, `tools/`, `scripts/`.
- Infrastructure/contracts: `compose.yaml`, `deploy/`, `contracts/`, `.github/workflows/ci.yml`.
- Coordination: `docs/OWNERSHIP.md`, `docs/contract-gaps/TEMPLATE.md`, `docs/session-status/S00.md`–`S04.md`.
- Legacy `server/`: no changes.

## Data migrations

- Alembic anchor `s00_0001` exists and `alembic heads` reports `s00_0001 (s00) (head)`.
- S01 owns `s01_0001`–`s01_0999`; S02 owns `s02_0001`–`s02_0999`; S03 requests database changes via contract gap; S04 owns explicit merge revisions.
- PostgreSQL init creates the isolated `platform` schema. No old SQLite data is modified.

## Commands and results

- `pnpm dev`: passed; one command started/reused all healthy dependencies, FastAPI, Temporal worker and four web applications.
- `pnpm check`: passed.
  - TypeScript lint/typecheck: 11/11 workspace tasks passed.
  - Unit tests: API client 1 passed; Python 3 passed; empty package/app suites explicitly pass without fabricated tests.
  - Python Ruff format/lint and strict mypy: passed.
  - Four production SPA builds: passed; each generated `build/client/index.html`.
  - OpenAPI regenerate/hash comparison: passed.
  - Legacy-route hash protection: passed.
- `pnpm smoke`: passed; PostgreSQL accepts connections, Redis returns PONG, ClickHouse returns Ok, Temporal reports SERVING, MinIO and Temporal UI return success.
- Runtime workflow: `PlatformHealthWorkflow` returned `geo-platform-v2:runtime-verified`.
- Runtime HTTP: API, readiness, four application routes, Temporal UI, MinIO and ClickHouse all returned HTTP 200.

## URLs

- API health: `http://127.0.0.1:45200/api/v2/health`
- API readiness: `http://127.0.0.1:45200/api/v2/readiness`
- Customer: `http://127.0.0.1:45101/platform/customer/`
- Operations: `http://127.0.0.1:45102/platform/operations/`
- Reports: `http://127.0.0.1:45103/platform/reports/`
- Intelligence: `http://127.0.0.1:45104/platform/intelligence/`
- Temporal UI: `http://127.0.0.1:18080`
- MinIO API/console: `http://127.0.0.1:19000` / `http://127.0.0.1:19001`
- PostgreSQL/ClickHouse/Temporal/Redis: `127.0.0.1:55433` / `18123` / `17233` / `16380`

## Evidence

- `tests/runtime-verification.json`: UTC timestamp, nine HTTP checks, seven healthy Compose services and executed Temporal workflow result.
- `tests/compose-smoke.json`: machine-readable Compose service state.
- `contracts/openapi.json` and `packages/api-client/src/schema.generated.ts`: authoritative API and generated client.
- `contracts/generated-manifest.json`: drift baseline.
- `contracts/legacy-route-manifest.json`: old-route protection baseline.

## S00 completion gates

1. PASS — all required directories/projects exist.
2. PASS — all Compose dependencies are running and healthy.
3. PASS — FastAPI, Temporal worker and four web shells ran; runtime evidence includes HTTP/workflow results.
4. PASS — generated OpenAPI client compiles in the workspace and is imported by all four shells.
5. PASS — `pnpm check` and `pnpm smoke` passed.
6. PASS — frozen conventions, OpenAPI-aligned mock, ownership and migration ranges let S01–S03 begin independently.
7. PASS — this status records files, commands, tests, URLs, evidence, risks and next action.

## Risks

- The host has global HTTP proxy variables; local smoke commands therefore explicitly bypass proxies. Container health checks use native clients or disable proxying.
- Docker Hub had intermittent EOF during the first image download. The final images are present and all services are healthy; future clean hosts may need registry retry/mirror handling.
- The surrounding workspace has no Git repository metadata, so drift and old-route protection use committed SHA-256 manifests instead of `git diff`.
- Contract mock behavior is deliberately minimal and must be replaced by S01/S02 implementations without changing the frozen response shapes silently.

## Next action

- Start S01, S02 and S03 in parallel using `docs/OWNERSHIP.md`, the frozen contracts/mocks and their respective session-status files.
