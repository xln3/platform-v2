[Service]
# Production compatibility drop-in for an already-installed base unit.
# A fresh install uses the EnvironmentFile line in geo-platform-v2-worker.service.
EnvironmentFile=/etc/geo-platform-v2/worker-adapters.env
EnvironmentFile=-/etc/geo-platform-v2/wukong.env
UMask=0077
