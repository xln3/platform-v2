[Service]
# Two API workers idle near 320 MiB. A full customer-dashboard projection adds
# about 100 MiB transient RSS, so the former 512 MiB throttle was below the
# measured concurrent working set and caused Uvicorn health-check terminations.
MemoryHigh=1G
MemoryMax=1536M
OOMScoreAdjust=-500
