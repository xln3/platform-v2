# This file is mandatory for the production unit and is loaded after
# platform.env.  Report production validates a frozen publishable snapshot
# binding and fails closed before rendering when that binding is invalid.
GEO_ENV=production
GEO_REPORT_TEMPORAL_TASK_QUEUE=geo-platform-v2-report
