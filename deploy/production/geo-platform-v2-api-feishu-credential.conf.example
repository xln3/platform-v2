[Service]
# The API only needs the independent HMAC key used to verify assist card links.
# It does not receive the App Secret, Encrypt Key, or tenant access token.
LoadCredential=feishu-link-signing-key:/etc/geo-platform-v2/credentials/feishu-link-signing-key
