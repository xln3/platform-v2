[Service]
# Install this drop-in only while the legacy Server酱 fallback remains enabled.
# Remove it after GEO_ALERT_NOTIFY_CHANNEL=feishu_app has passed the migration window.
LoadCredential=alert-sct-sendkey:/etc/geo-platform-v2/credentials/alert-sct-sendkey
