[Service]
# BrandRank's legacy audit projection may fill extraction-cache misses while
# serving an operator read. Release directories are immutable, so keep that
# mutable cache in the persistent runtime tree explicitly allowed by the API
# service sandbox.
Environment=GEO_BRANDRANK_EXTRACT_CACHE_DIR=/home/xln/geo-system/platform-v2/runtime/brandrank-extract
